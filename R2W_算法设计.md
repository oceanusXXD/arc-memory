# R2W: 以读定写 —— 先测出来，再决定一条记忆该怎么存

**R2W: Read-Driven Writes — Measure the Value of a Memory Before Deciding How to Store It**

> **本版修订说明(GBM 版;投稿前可整段删除)。** 本版为定点修订,不是重写:① 预测器统一写为 GBM,具体 boosting 实现由验证集确定,不把 LightGBM/XGBoost/CatBoost 中的某一个写进方法定义;② 第一关输出统一为 $(\hat y_b,\hat\sigma)$,成本由确定性计算器 $\bar C$ 合成;③ 第二关统一为逐臂预测 $(\Delta\hat E_p,\hat\rho_p,\hat\sigma_p)$,构建与索引成本直接计数,读取成本由 $L^{\text{ctx}}_p\hat\rho_p$ 合成;④ 同步修正 §4.3、§4.5、§7.1.1 与附录中的旧版 $\hat y_c/\hat C$ 记号。双轴分解、三层测量、五步成签、分量入库、两行决策与 none 的四条硬定义均不改。

---

## 摘要

长期对话智能体需要在每一轮对话到达时,当场决定这一条记忆**存不存、以及存成什么形式**。困难在于:做决定的这一刻,将来的查询完全不可见。存得太粗,将来被问到细节就答不上;存得太全,既浪费存储和读取,又可能把真正需要的证据从检索结果里挤出去。现有做法要么依赖固定规则或让语言模型临时判断,要么用强化学习拟合稀疏的下游奖励。前者对"存错了会怎样"没有度量也没有保证;后者训练昂贵、奖励稀疏,而且冷启动容易退化成"什么都不存"。

本文观察到:记忆系统的检索索引可以从对话记录完全重建。这意味着我们不必靠猜测或试错——可以在离线阶段把"这一条存成每一种形式"的结局真正跑一遍、量出来。由此,写入决策从"经验判断"变成"直接测量"。需要说明的是,这里指的是在"写一次、固定检索器、只做局部动作选择"的设定里得到 per-decision full feedback,因此不需要强化学习;一旦引入更新/合并/删除/巩固等会跨时间步改变索引的动作,结论即失效。

本文提出 R2W(Read2Write,以读定写)。R2W 把写入决策拆成两个不同的决策轴:先判"存不存"(存在轴),再判"存成什么形式"(形式轴)。系统用统一粗筛、单条替换、多背景审计三层离线测量,对每个通过粗筛的候选动作真实重放。价值的唯一结局是**端到端答题正确率**(同题配对差分;检索指标只作诊断与归因护栏,不进价值函数),干扰成本与三类资源成本同场测出,标签按"效应 + 成本"分量入库、决策时才按显式声明的汇率折算——换汇率不换标签。判定"不存"的记忆零热索引足迹、原文进冷归档可随时恢复,并保留 5% 影子索引继续观测,判断错了可以纠正。两级预测器全部是小模型,主线采用 GBM,具体 boosting 实现由验证集确定:第一关用内容特征加一次 ANN 索引探针,回归"最优臂的收益"与不确定度两个标量、成本由确定性计算器合成(能数的不学),约 76% 的轮次在此零调用终止;第二关先出题、再对 2–3 个短名单候选做确定性**草稿构建**与事实证书预检,由逐臂打分的 pooled GBM 对每个候选存法预测效应与命中率,再与精确计数的构建/索引成本合成净值,在"存原文与持证草稿"之间取净值最高者——整个在线决策只有两行规则,不调用大模型,也不含任何需要校准的门槛。岭回归、案例回归与小网络作为审计基线,若更简单的方法稳定胜出则按预注册规则替换。大模型只出现在离线测量(复用部署自身的阅读模型与判分器,属一次性成本);构建调用集中在约 24% 的放行轮次,先验约为全量抽取基线的 6% 量级。

**关键词**:长期对话记忆;写入策略;全信息测量;潜在结果与干扰;不确定度感知决策

---

# 1 引言

## 1.1 问题的背景:写入时看不到未来

一个长期对话智能体,往往要在跟用户聊了几百轮之后,再回头回答用户的问题。为了让回答有依据,系统需要把历史交互写进外部记忆,并在查询到达时检索出少量相关内容交给阅读模型作答。

每来一轮对话,系统都要立刻回答两个问题:

> **这一条内存不存?存成什么形式?**

这两个问题必须在一个很不利的条件下回答:此刻,未来的查询完全不可见。而选择又会同时影响三件事:第一,写入要花多少 token;第二,将来每次检索要读回多少 token;第三,将来这条记忆能不能被搜到、搜到之后能不能帮助答对题。

更麻烦的是,所有记忆共享同一个排序索引。每写进一条新记忆,它都可能把别的真正需要的证据往下挤。所以"存得越多越好"并不成立:存得太粗,将来被问到细节就答不上;存得太全,既费存储、费检索、费读取,还可能挤掉别人的证据。

一句话概括,这是一个"事前配置"问题:在信息不完备的条件下,决定每一份记忆该占多少检索容量。它很像数据库设计里的物理设计问题——但区别在于,数据库的索引顾问能看到未来全部的查询负载,而记忆系统在写入的那一刻看不到任何未来查询。

## 1.2 现有方法的四个问题

我们回顾现有系统,指出四个值得点名的问题。

**第一个问题:决策没有可问责的依据。** Mem0 [15]、A-Mem [16]、MemOS [13] 这类系统在写入时靠预设规则,或者干脆让语言模型当场拍板。它们对"存错了会怎样"没有度量,也没有任何保证。一个方向正确但仍有缺口的例子是 A-MAC [1]:它把准入拆成五个因子(未来效用、事实置信、新颖度、时近度、类型先验),这五个因子的思路是对的;但它的原文自己承认,记忆准入至今是 agent 架构里"a poorly specified and weakly controlled component",而且其中的效用项只是单次语言模型启发式打分。这正好印证了我们的判断:结构有了,但依据仍不是测出来的。

**第二个问题:强化学习方案既贵又脆弱。** Memory-R1 [2] 和 AgeMem [3] 用强化学习(PPO/GRPO)训练记忆管理器,依赖稀疏的下游奖励反复试错。代价是两层:训练很贵;而且冷启动极易塌缩成"什么都不存"。后者不是猜测,而是被实证确认过的——MemRouter [4] 的附录给出:纯 GRPO 不作预热时 F1 只有 1.1,策略退化成全部返回"不存";混入 UPDATE 标签又会过预测、把记忆库塌缩掉。更值得注意的是,直到 2026 年 8 月,RoMeRL [5] 整篇论文还在修复强化学习记忆里的"memory-reward trap"(奖励被同一批共同检索出来的记忆错误分摊)。这说明奖励分配至今仍是强化学习记忆管理的活跃难题,也是本文转向测量的直接原因。

**第三个问题:写路径过滤是全行业最薄弱的环节。** 2026 年 3 月的一篇领域综述把 write-path filtering [6](存不存)与 learned forgetting(学习的遗忘)一起列为开放问题。现状是,"不存"只是语言模型操作集里被淹没的一个选项;没有哪一个系统系统性地把"不存"的收益测出来——没有零点,就没有对照。

**第四个问题:不算干扰账,也没有任何写入质量保证。** 检索增强生成领域早就量过"语义相关但不含答案"的干扰段落对答案的伤害 [7,8],但没有任何记忆系统把单条记忆造成的干扰成本测出来并算进决策账,也没有哪一家给过写入质量的形式化保证。

## 1.3 本文方法的核心思想

我们的出发点是:记忆系统不是一个物理世界,而是一个可以从对话记录完全重建的索引。这意味着,今天选错了存法,明天可以推倒重来,把另一个选择也真跑一遍。于是:

1. **候选动作先粗筛,筛过的都真的跑。** 对每条记忆,先做全库统一重放的粗筛;"不存"也跑、"存原文"跑、存活的增强存法各跑一遍,每种选择的结局(将来搜不搜得到、搜到了能不能答对、会不会干扰别人)直接量出来,不用猜。粗筛只剪枝、不产出标签,**最优动作被误剪的概率**是必须公开报告的诊断量。

2. **既然每条记忆的每个候选动作都能测,就不需要强化学习。** 用监督学习学一个打分器就好——但只限"写一次、固定检索器、只做局部动作选择"这一设定。

3. **"存不存"和"存成什么"是两个独立的轴。** "存不存"看净账:这条记忆自己能带来多少收益,减去它将来造成的干扰和读取开销。`none` 不是"剩下的那个选项",而是一个被明确定义为零、且收益被测量的对照点。

4. **"存成什么"** 则先对 2–3 个短名单候选做**草稿构建**与证书预检,再由逐臂打分的小模型在"存原文与持证草稿"里选"预测收益减成本罚金减不确定罚金"最高的;没有任何加工臂挣得过原文,就存原文,确保信息永远完整。

5. **判定"不存"并不删除。** 原文进冷归档随时可取,还保留一小部分影子通道继续观测。这样,判断错了可以纠正、可以恢复。

6. **决策只有两行,没有要校准的门槛。** 存在轴:预测净账加保守项不为正,就不存;形式轴:在"原文 + 持证草稿"里取净值最高者,没有比原文更赚的就存原文。保守方向是刻意的:预测越不确定,决策越往"存、存原文"的方向偏;构建契约与事实证书是构建期的硬检查,判错的 `none` 也能从冷归档捞回。

## 1.4 本文的贡献

1. **把同一条记忆的写决策,拆成两个不同的反事实现应。** 存在轴回答"$p$ vs 不存"($\Delta^{E}_p$),形式轴回答"$p$ vs 存原文"($\Delta^{F}_p$)。两条轴都定义为**同一个查询分布** $\mathcal D_Q$ 上的期望差异,因此相关收益、无关干扰、读取成本可在同一单位下直接相减。这不是简单地把两个轴并列,而是把"占用不占用检索容量"和"以什么形式占用"作为**两个不同的干预对照**各自测量。

2. **把共享检索索引带来的负外部性显式放进写入价值。** 记忆 $t$ 的加入不仅影响问它自己的问题,还可能把别人的证据从前 $k$ 名挤出去。R2W 把这条干扰量化为 $\Psi_p$(并乘上无关查询概率)计入净存储价值,而不只是"相关度/新颖度"之类的启发式分。这是与 MemRouter、A-MAC、FluxMem 最实质的差别。

3. **用离线反事实重放替代 teacher 标签。** 既然索引可重建,就对每条记忆先粗筛、再对存活的每个候选动作真正改变索引、重跑下游,量出**逐动作的反事实效应向量**(而不是一个 argmax 类别标签,也不是 teacher 生成的 ADD/NOOP 标签)."全测"受成本约束故先粗筛,最优动作被误剪的概率如实报告(§4.1.2、§7.3.2)。这正是 MemRouter 用 teacher 标签、FluxMem 用单一 argmax 结构所缺的一步。

4. **不确定度感知、且完全可逆的写入决策。** 决策只有两行:存在轴"净账不为正就不存"(没把握就存),形式轴"原文与持证草稿同台取净值最高"(没把握就存原文);`none` 进冷归档而非删除、保留影子观测,任何错误决策都可恢复。收益与成本**分头计量、决策时按显式声明的汇率折算**——可数的成本(构建与索引 token、正文长度)精确计数,需要学习的只有效应与检索命中率;换汇率不换标签、也不换模型(一次性与存量成本按声明摊销基准 $N_Q$ 折成每查询单位,§3.5)。我们刻意不引入需要校准的门槛层:评估过把 risk calibration 家族搬到写侧的方案,因"被校准的规则难以与真实执行的规则同一"而放弃(§4.3)。

> **关于"全信息测量使强化学习不再必要"**:这句话要限定范围。在"写一次、固定检索器、只做局部动作选择"的设定里,per-decision full feedback 成立,强化学习确实不用;但一旦引入更新/合并/删除/巩固等会跨时间步改变索引的动作,就不成立了。本文的结论只在该设定内成立,相关讨论见 §2.3。

---

# 2 相关工作

## 2.1 长期对话记忆系统

长期对话记忆已经是语言模型 agent 的标准组件,而且架构越来越分化。本文把既有系统按四条脉络梳理——**分层与调度、非参数检索记忆、结构化与组织、上下文侧记忆组织**——再给出 R2W 与它们的精确分工。

**分层与调度方面**:MemGPT [11] 把上下文管理抽象为显式换入换出的分层存储,由 LLM 自己用函数调用决定何时把内容从主上下文换出到档案存储——它是"agent 自主管记忆"的滥觞,但换入换出的判据完全交给模型启发式,没有度量也没有保证。MemoryOS [12] 借鉴操作系统内存管理,设计"短期—中期—长期"三层存储,以 FIFO 与基于热度的分段分页在中长期间迁移,并确立了"归档而不删除"的思想——但其淘汰触发条件是热度启发式,不回答"这条记忆还值不值得占检索容量"。MemOS [13] 以 MemCube 统一明文、激活、参数三类记忆,并提供记忆调度 API;它的贡献在"记忆的统一抽象与调度",写侧准入判据不在其范围内。MemoryBank [14] 把 Ebbinghaus 遗忘曲线引入记忆衰减,让内容随时间自然降权——它回答的是"多老的记忆该降权",而不是"这条具体的记忆值多少钱"。

**非参数检索记忆的更早谱系**:把"可增删的外部记忆分片 + 最近邻检索"做进语言模型,可追溯到 kNN-LM [70](把下一个词的分布与最近邻检索结果插值)、RETRO [71](从万亿 token 级文库检索文本块、注入注意力)与 Memorizing Transformers [72](在层内缓存 key-value、以 top-k 近似把有效上下文推到十万 token 级)。这批工作证明了"可检索的外部记忆"自身就有收益,但记忆内容由语料预处理固定,不存在"要不要写、写成什么"的决策——R2W 面对的问题,恰好是记忆从"语料"变成"对话交互产物"之后才出现的。MemoRAG [73] 往前再走半步:先由全局记忆模型对开放查询生成检索线索,再由线索去检索——记忆开始参与检索规划,但写入侧仍由离线预处理决定,没有写侧决策层。

**结构化与组织方面**:Mem0 [15] 采用事实抽取、整合、检索的流水线,并提供图记忆变体;写入时由抽取器与大模型合并决策驱动,判据是启发式。A-Mem [16] 借鉴 Zettelkasten 笔记法,在写入时为每条记忆生成属性、关键词与标签,并动态建立记忆之间的关联——组织是强项,但"该不该入库"仍交给语言模型当场判断。Zep [17] 用时序知识图谱组织记忆,为关系三元组引入时间窗并处理事实失效,是"记忆会过期"这一工程现实的代表。AriGraph [18] 结合语义图与情景记忆双通道,面向探索类环境;MIRIX [19] 把记忆拆成核心、情景、语义、程序化等多种类型,用多组件管线协调;O-Mem [20] 以主动用户画像组织记忆,面向个性化与自演化 agent。SeCom [21] 提出段级记忆单元并以压缩去噪提升检索;MemGAS [22] 组织多粒度单元、用熵驱动路由器在**检索时**选粒度——注意它的粒度选择发生在读侧,写侧仍是固定的;HippoRAG 2 [23] 用图增强检索把海马体式的联想记忆引入读取;Infini Memory [24] 以主题文档管理长期生命周期。此外还有一批近期系统:MemoChat 让模型把对话对照写成结构化 memo 供后续检索,是"写入时抽取"的早期形态;LightMem 以低开销记忆增广为设计目标,关注点是把记忆管线本身的代价压低;SimpleMem 主张少组件、易部署的终身记忆管线;MemEvolve 把记忆库做成可跨会话自演化的知识库;Cognee 提供开源的图记忆后端与语义层(以上系统的出处统一登记,见文献 [25])。共同点很清楚:它们设计的是"记忆怎么组织、怎么检索",而不是"这一条该不该存在"。

**上下文侧的记忆组织**:另有一路工作不建外部记忆库,而是把长上下文本身情节化:ReadAgent [74] 仿人类阅读,把长文分页、逐页压成 gist,查询时再回翻原文;EM-LLM [75] 用贝叶斯惊奇把上下文切成情节事件,检索时结合连续性呼应与刻意回想。它们相当于永远只使用一种存法(固定策略的摘要/gist),且该策略不参与任何测量与校准;R2W 的形式轴把"存成什么"变成逐条测量的十种存法选择,`sum` 存法与 gist 思想同族,但它需要用测量挣得入场,而不是默认策略。

一篇从数据管理视角出发的综述 [26] 把这批系统按"表示、抽取、路由、维护"四个模块统一拆解,发现没有一个架构在所有场景里都占优,而且最稳定的写侧做法是"覆盖保持的写入时抽取"——也就是抽出能覆盖原文、又不造成破坏性压缩的表示。这个观察与我们把"压缩收益"和"检索收益"分开记账的立场是一致的。

R2W 与这批工作是分工而非替代:它们决定"记忆是什么、怎么组织、怎么检索";R2W 决定"这一条具体记忆该不该占容量、以什么形式占"。两点具体衔接:其一,采纳 MemoryOS 的归档不删除思想,但把降级的触发条件从热度启发式换成测出来的净存储价值;其二,采纳 Zep 的时序图思想作为 `graph` 卡,但规定图单元只做索引卡、答案一律回正文取。

## 2.2 写侧准入与表示选择:从启发式标签到结果测量

把"存不存"从操作集里独立出来,做成一个专门的决策结构,这条路线各家都在做:MemRouter 的 {ADD, NOOP} 解耦,A-MAC 的五因子准入,Dual-Layer [30] 的三分写入路由,ConsistencyGate [31] 的保真闸门。**结构大家都想到了,缺的是两件东西:要么标签靠 teacher 蒸馏或启发式(拿不出"为什么值得"的账),要么缺乏可核查的决策结构。** 共同缺口是:除 FluxMem 等个别工作在候选结构层面做过离线结果测量外,收益从未被系统测量;写侧干扰从未被计入;写入价值也从未被挂到"不存"与"存原文"这两个天然对照点上。

在这里需要特别公平地看待 **FluxMem** [27]。它确实已经做到"离线运行多种候选记忆结构、用下游响应质量生成监督",也就是**它在候选结构层面已经做了离线结果测量**;它与本文的差别在于:(i) FluxMem 把每个会话/交互的候选结果**压成一个 argmax 结构标签**再训练分类器,而 R2W 保留逐动作的筛后反事实效应向量 $\{\Delta^E_p,\Delta^F_p,\Psi_p,C_p,\sigma_p\}_{p\in\mathcal P}$ 并学习排序与不确定度;(ii) FluxMem 没有 `none` 这一存在轴反事实(它的动作集里没有"完全不占容量"这一项);(iii) FluxMem 没有对写作干扰(`none` 之外还有"当前这条记忆会挤掉别人")与不确定度保守的决策保护(保守项、硬回退)。这三点正是本文相对 FluxMem 的差异,而非"离线跑一遍"这一个想法本身。

**预算与压缩档位**:BudgetMem [28] 用强化学习在读侧做运行时预算路由,把已存进的记忆内容按多档位压缩、读到什么算多少。它与 R2W 恰成镜像:它是"读侧在已存内容上选看多少",R2W 是"写侧决定要不要存、存成什么";R2W 的压缩轴(`sum` 一族)覆盖其档位轴,但写入档位由测量而非策略梯度决定,且把干扰与保守项一并入账,不像它那样以强化学习的收敛脆弱性为代价。

**双轨与事后追认**:MemSIF [29] 从结构化交互构建双轨事实记忆(CoreFact/ActiveFact),并给出两个有用的概念:TSM(写入时显著性可以预测的用途)与 DUM(写入时预测不了的用途)。它的事后追认机制被 R2W 吸收为读侧兜底(§4.5)——承认写侧事前预测必有覆盖不了的残余需求,而不是假装可以完备。但它本身不做写侧测量、不把干扰入账。

**三分写入路由**:Dual-Layer [30] 把每个写入决策分成 non-write / write-new / write-update 三路,再配慢速巩固。它的 non-write 路与本文存在轴最接近,但三路判据基于冗余检测与启发式规则:收益无测量,风险无校准。

**保真闸门**:ConsistencyGate [31] 在写入侧强制自一致性检查,防止污染性事实入库——它查的是"这条内容真不真",并且明确声明不查"这条内容值不值得";其阈值(建议 τ=0.7)为手调、无校准。事实保真是 R2W 证书层关心的另一半(§4.1.3),但"真的"不等于"值得的"。

**表 1｜与候选系统在五个能力维度上的对比。**("收益可测"指写侧动作的收益是否被离线测量,而非估计或启发式。)

| 工作 | 准入解耦? | 收益可测? | 形式轴? | 干扰入账? | 统计保证? |
|---|---|---|---|---|---|
| Mem0 / A-Mem / MemOS | 否(不存淹没在操作集里) | 否 | 否 | 否 | 否 |
| MemoryOS | 部分(热度降级) | 否 | 否 | 否 | 否 |
| MemRouter | 是(二元准入解耦) | 否(teacher 蒸馏标签) | 否 | 否 | 否 |
| A-MAC | 是(五因子准入) | 否(单次语言模型启发打分) | 否 | 否 | 否 |
| FluxMem [27] | 否(无 `none` 存在轴) | 是(候选结构层面离线测量,但压成 argmax) | 是(结构,单点取最大) | 否 | 否 |
| BudgetMem [28] | 否 | 否 | 是(压缩档位,强化学习) | 否 | 否 |
| MemSIF [29] | 否 | 部分(事后追认) | 部分 | 否 | 否 |
| Dual-Layer | 是(三分路由) | 否(冗余/启发式) | 部分(级联) | 否 | 否 |
| ConsistencyGate | 部分(保真闸门) | 否 | 否 | 否 | 无(阈值手调) |
| Memory-R1 / AgeMem | 是(操作集内) | 否(强化学习奖励,只见一条路) | 部分 | 否 | 否(收敛依赖难验证的条件) |
| MemGuard [32] / SSGM [33] | 部分(可信性/巩固前校验) | 否(关注可信而非价值) | 否 | 否 | 否 |
| StateMem [34] | 否(管被取代的状态) | 否 | 否 | 否 | 否 |
| **R2W(本文)** | **是** | **是(测量净价值)** | **是(十种形式轴)** | **是(干扰 Ψ 入账)** | **否(不声称;以硬契约 + 证书预检 + 冷归档可逆替代,§4.3)** |

## 2.3 记忆管理中的强化学习,以及为什么本文不用它

Memory-R1 用 PPO/GRPO 训练记忆管理器和回答 agent,只用 152 个问答对就在多个基准上超过强基线;AgeMem 把存储、检索、更新、摘要、丢弃五个操作工具化,用三阶段(监督预热、任务级强化学习、步级 GRPO)优化,并学到了诸如"在上下文填满前主动摘要"这样的非显然策略。

这类方法之所以必须用强化学习,是因为它们的奖励要跑完整的"存、检索、生成"流水线,而且只能看到实际走的那一条路——这正是 bandit feedback 的定义。而 R2W 的离线阶段是 full feedback:索引可推倒重建,所以**对某一条记忆,在固定的背景下,它的任意候选动作(包括 `none`)的结局都可以反事实重放、量出来**。这才是本文所说"full feedback"的准确含义——是**每个动作的单个结局在原则上都可测**(全测一遍是成本问题:实践中先做全库统一粗筛,再对存活动作逐一单条重放),而不是"整个 $|\mathcal A|^n$ 种全局联合赋值都能测得"。因此,在我们限定的"写一次、固定检索器、局部动作选择"这一设定里,强化学习确实没必要;但一旦加入**更新、合并、删除、巩固、记忆状态转移、长程容量约束**这类会跨时间步改变索引结构的动作,强化学习或动态规划仍然可能是合理工具。本文的结论应缩小到这一设定内。

这条线到今天仍在被持续修复:MemRouter 附录给出纯强化学习冷启动塌缩为全不存(F1=1.1)、混入 UPDATE 模式坍缩;2026 年 8 月的 RoMeRL 仍在修强化学习记忆的 memory-reward trap(轨迹级奖励被共检索出来的记忆错误分摊)。这也解释了为什么本文选择放弃强化学习、转向全信息测量。

## 2.4 评测基准、状态追踪与记忆生命周期

**评测基准方面**。除了主要使用的 LoCoMo [64] 与 LongMemEval [65] 之外,2025–2026 年出现了一批把"记忆加行动"绑定的评测:MemoryAgentBench [37] 从准确检索、测试时学习、长程理解、选择性遗忘四项认知能力刻画记忆;MemoryArena [38] 设计跨会话、相互依赖的 agentic 任务,发现已经接近饱和的 LoCoMo 模型在这里掉到 40–60%;MemBench [39] 区分事实型与反思型记忆、参与与观察两种模态;LongMemEval-V2 [40] 面向 web agent 的经验内化(上下文最深 115M token);AMA-Bench [41]、EvoMemBench [42] 分别测长视界记忆与自演化视角。这些基准的共性是:它们测"记忆系统整体输出好不好",而不测"写入侧这一步值不值"。这正是本文要补的那一块空白——R2W 不把记忆当作黑箱,而是把写侧每个动作的收益拆出来、测出来、可复核。

**状态追踪与记忆生命周期方面**。一个有效的记忆系统不仅要"记对了",还要"跟上变化"。StateMem/StateMemBench [34] 用封闭集合评分,把"当前状态、被取代状态、其他失败"分开,指出现有基准偏重回忆型任务。它的立场与本文互补:R2W 的两个轴决定"这条记忆该不该进、以什么形式进",但并不追踪"已被取代的状态",完整的超期处理与 StateMem 这类工作是互补而非替代。留存侧的证据同样支持本文的两处设计:保留原始 episode 的记忆比强制巩固者的准确率高约两倍 [43];rate–distortion 视角给出"查询无关压缩必付惩罚"的下界,以及"可逆归档弱优于不可逆丢弃"的原则 [66]。这两条分别支撑了 R2W 的 `raw` 兜底(信息完整、不激进改写)与冷归档而不删除(判断错了可逆)。治理与写侧可信性方面,MemGuard 把验证器信号(奖励、置信、标签、不确定度)作为持久化元数据,在检索、冲突消解、摘要、归档各阶段复用;SSGM 在巩固之前强制一致性校验、时间衰减与访问控制——它们关注"入库内容是否可信",本文关注"入库收益是否超过成本与外部性",二者可以叠加。

## 2.5 读侧风险控制与数学工具

把"不确定就不做"形式化的努力,几乎全部集中在读侧。RSCB-MC [35] 把记忆使用重构为风险敏感控制,把"弃权"列为记忆控制器的合法动作;conformal 路由 [10] 用分布无关的校准来门控模型选择;Conformal Arbitrage [9] 用 CRC 校准 Primary/Guardian 仲裁阈值。**工具相同,位置不同**——本文把同一套工具从读侧移到写侧,并额外承担读侧不存在的困难:写入动作之间存在干扰,因此"这个动作好不好"必须相对于背景来定义。

在数学工具上,本文的做法是逐族登记出处、各归其位:全部工具都来自成熟文献,本文的组合点是**位置**——把它们从读侧与下游评测搬到记忆写入侧,并处理写侧特有的干扰。每一族在 R2W 中的具体用途如下:

| 工具族 | 在 R2W 中的用途 | 出处(逐条) |
|---|---|---|
| 潜在结果框架 | 写入价值的反事实定义;"索引可重建 ⇒ 结局可测" | Rubin 1974;Imbens & Rubin [36] |
| 网络干扰 | SUTVA 违反的正式表述(结局依赖全库赋值) | Ma & Tresp [44] |
| 设计导向的效应估计 | 随机混存实验的偏差审计 | Aronow & Samii [45] |
| PU / nnPU(仅附录扩展) | 纠正 benchmark 问题稀疏的有偏引用标签 | Kiryo et al. [50];Elkan & Noto [51] |
| DR / SNIPS | 在线闭环的收益纠偏(长尾自归一) | Dudík et al. [52];Swaminathan & Joachims [53] |
| 特权信息蒸馏 | 未来使用画像 u(t) 的标签框架 | Vapnik & Vashist [54] |
| LambdaRank | 形式轴效应头的成对排序损失(仅同窗内比较) | Burges [55] |
| 异方差不确定度 | 打分公式的风险罚金与存在门放行 | Kendall & Gal [56] |
| LETOR 四族划分 | M/Q/I/E 四族特征的"文档、查询、匹配"分解 | Qin & Liu [57] |
| BM25 / RRF | 检索配置与融合的公开、可复现参数族 | Robertson & Zaragoza [58];Cormack et al. [59] |
| HyDE | 以伪文档引导检索的思想,hq 卡设计同源 | Gao et al. [60] |
| doc2query / docTTTTTquery | 查询扩展:生成问题作索引卡素材 | Nogueira & Lin [67] |
| Doc2Query-- | 生成问题必须过滤 ⇒ 本文的合格性过滤 | Gospodinov et al. [68] |
| 拼接伤稠密检索研究 | hq 卡对稠密检索需独立编码、不拼接 | [69] |
| FActScore | 原子事实拆分 + 逐条比对的证书范式(§4.1.3) | Min et al. [61] |
| AlignScore | 证书比对的 NLI 蕴含评分 | Zha et al. [62] |
| MontageLie | 已知的比对盲区("真话拼成假话") | [63] |
| 处理效应元学习(S-learner) | 第二关逐臂效应学习的范式:pooled 单模型 + arm 特征(§4.2.3) | Künzel et al. [76] |
| 成对 LLM 判分的可靠性 | 开放类答案的差值评分协议(位置互换去偏,§6.2) | Zheng et al. [77] |
| 梯度提升模型(GBM; LightGBM 为一种实现) | 效应学习的候选模型族之一,具体实现由验证集确定(§4.2.3) | Ke et al. [78] |

注:上表只登记 R2W 实际使用的工具。我们也评估过把风险校准家族搬到写侧门槛的方案——CRC [46]、分裂/分层 conformal [47]、选择性的 SCRC [48]、漂移下的 ACI [49]——最终放弃,理由写在 §4.3:被校准的门槛与真正执行的决策规则难以同一,且策略一更新、门槛即失配,运维复杂度高于换来的保证价值。本文改用两行决策规则(不存的零净账、原文与持证草稿同台比净值)+ 不确定度保守项 + 构建期硬检查的结构。

# 3 问题定义

## 3.1 数据、使用轨迹与写入动作

一段对话记为 $c=(T,Q,E)$。$T=(t_1,\dots,t_n)$ 是按会话与时间排序的轮次; $Q$ 是这段对话之后的查询集合; $E(q)\subseteq T$ 是回答 $q$ 所需的证据轮次。我们定义训练期可见的**使用集合**与**被引用指示**:

$$U(t)=\{q\in Q:\ t\in E(q)\},\qquad S(t)=\mathbb 1[U(t)\ne\varnothing].$$

$S(t)=1$ 只表示这条记忆被**当前 benchmark 的有限查询集**用到。它不能解释为"在所有未来场景中有用",也不能把 $S(t)=0$ 解释为"没用"。因此本文自始至终以可观测的测量量为标签,不把 $S(t)$ 当成潜在"未来有用性"的估计值——这是本文的一个关键立场(见 §4.2.2)。

**写入动作**是两轴的乘积:

$$\mathcal A=\{\texttt{none}\}\ \cup\ \mathcal P,\qquad
\mathcal P=\{\texttt{raw},\texttt{raw+kv},\texttt{raw+event},\texttt{raw+graph},\texttt{raw+hq},
\texttt{sum},\texttt{sum+kv},\texttt{sum+event},\texttt{sum+graph},\texttt{sum+hq}\}.$$

$\texttt{none}$ 是存在轴(存在轴)的零点;$\texttt{raw}$ 是形式轴的基准。整段对话的写入赋值为 $a\in\mathcal A^{n}$。

## 3.2 两个决策轴的正式定义

存在轴回答"这条记忆要不要占用任何检索容量";形式轴只在"存"被选中时才有意义,回答"以什么形式占用"。两轴对应两个不同层级、不同对照的决策问题。形式轴的十种存法由**压缩开关**(保留原文 / 压成不超过 60% 的摘要)与**索引卡类型**(不加卡 / kv 键值对 / event 时间线 / graph 关系 / hq 生成问题)因子化而成,见表 2。所有存法的索引卡总量不超过 64 词/轮,摘要不超过原文的 60% 并在句边界处打包截断。**契约在构建时强制执行——测量对象等于上线执行对象。**

每条被存的记忆都携带**公共信息块** $M(t)$:说话人、绝对时间(由对话日期锚解析,"上周六"保留原文的同时附上类似 `=[2023-03-14]` 的锚点)、会话编号、轮次编号。所有存法一视同仁地携带、都计费、读时模型都可见。之所以必须这样,是因为如果只有 `event` 存法带时间,那么"event 在时间题上赢"就无法区分是时间线化的功劳,还是仅仅因为它带了时间戳;统一提供之后才是公平对照。

**表 2｜十种存法(形式轴)与三类账本。** 契约在构建时执行。

| 存法 | 正文 | 索引卡 | 契约 | 类别 | 写入成本 | 读取成本 |
|---|---|---|---|---|---|---|
| `raw` | 原文 | 无 | — | 兜底基准 | 基准 | 基准(分数恒为 0) |
| `raw+kv` | 原文 | ≤4 条"实体: 值" | ≤4×16 词 | 买命中率 | 略多(卡增量 ≥0) | 与原文相同(卡不进上下文) |
| `raw+event` | 原文 | ≤3 条带时间锚短句 | ≤3×20 词(时间锚必保) | 买命中率 | 略多 | 与原文相同 |
| `raw+graph` | 原文 | ≤3 条关系三元组 | ≤3×20 词(须含值节点) | 买命中率 | 略多 | 与原文相同 |
| `raw+hq` | 原文 | ≤2 条生成问题 | ≤2×40 词(独立编码) | 买命中率 | 略多 | 与原文相同 |
| `sum` | 摘要 | 无 | ≤原文 60%,句边界 | 省钱型 | 更少 | 更少 |
| `sum+X` | 同摘要 | 同上各 X | 同上 | 兼得型 | 更少+卡 | 与摘要相同 |

一个数学事实:对一切增强存法,$|\mathrm{repr}_p(t)|=|\mathrm{repr}_{\text{raw}}(t)|+\Delta$,且 $\Delta\ge 0$。因此 `raw+kv` 这类**永远不可能节省构建 token**(原文没少,还搭了卡),它的价值只在"将来更容易被搜到"。如果把三类混在同一张"压缩前沿"图上画,会得出错误的归因结论——表 2 单独分三类就是为此。

## 3.3 以查询分布定义收益:一个结局、两类诊断、一条护栏

前面把这一轮拆成公共信息块 $M(t)$、正文 $P_p(t)$、索引卡 $K_p(t)$。现在最关键的一步,是把"这条记忆值多少分"重新落到**同一个单位**上,否则下面会遇到量纲冲突。

我们明确一个**部署时的查询分布** $q\sim\mathcal D_Q$。所有"收益""干扰""成本"都定义为在这个分布上的**期望**,而不是对某些查询求和、对另一些查询取平均。这样不同项才能直接相减。

**结局只有一个:端到端答题正确率。** 给定背景赋值 $b$(即其它记忆分别存成了什么形式),对任意查询 $q$,整条冻结读管线(检索 → top-$k$ → 阅读模型作答 → 评分)跑出答案,定义**查询级结局**:

$$u(q;b)\in[0,1]\;=\;\text{答案正确程度(有金答案的题用程序化 token-F1 等校验;开放类用成对差分 judge,协议见 §6.2)}.$$

> **白话**:判断"这条记忆值多少钱",只看一件事——存了它之后,系统答题是不是真的更对了。检索当然在管线里跑,但它只是通往答案的中转站,不是终点。

**为什么检索分不进价值函数。** 一个自然的想法是把检索质量 $g$ 与答题质量按某个权重(比如各一半)混合成综合分。本文明确拒绝这种做法,理由有三条:其一,**不可识别**——"检索分与答案分的兑换率"在任何部署数据里都没有对应的结局变量,原理上测不出来;其二,**双重计价**——检索失败已经通过 $u$(检索在闭环内测)传导进结局,再把 $g$ 加进目标,是对同一条因果路径收两次钱;其三,**权重学不出来**——要从数据学这个权重,先需要一个关于"(检索, 答案) 怎么权衡"的偏好数据集,而那本身就是待定义的效用,是循环论证。$g$ 的正确定位是**诊断与护栏**(下面两段),不是货币。

**诊断:检索与引用指标同报。** 归一化证据检索分

$$g(q;b)\;=\;\frac{\sum_{t\in E(q)}f(\mathrm{rank}_t(q;b))}{\sum_{t\in E(q)}f(1)}\in[0,1],\qquad f(r)=1/(r+2)$$

与条目命中、reader 引用正确性全程记录并同页报告:它们回答"收益是不是真的来自这条记忆被检到"这一归因问题,也是 §7.3.4 的 Pareto 轨迹与联合分布审计的两条轴;在线监控口径 $y_{\text{on}}$(§4.1.2)与离线逐字同式。

**护栏:不许以检索退步换答案进步。** 若某候选动作使 $\Delta u>0$ 但 $\Delta g<-\epsilon_0$(答案变好却伴随证据检索的明显退步——可疑、可能不可归因的"赢"),决策时按约束式规则否决该动作,默认 $\epsilon_0=0$。"不加护栏"与"加护栏"两个变体在 §7.3.4 同报。

写成期望单位:

$$\text{查询级收益}:\quad U_p(t;b)\;=\;\mathbb E_{q\sim\mathcal D_Q}\big[u\big(q;b[t\!\to\!p]\big)\big].$$

$b[t\!\to\!p]$ 表示"在背景 $b$ 中,把第 $t$ 条换成形式 $p$";$b[t\!\to\!\varnothing]$ 表示"换成不存在(不进热索引)"。于是存在轴与形式轴分别对应两**个不同的干预对照**。

## 3.4 两个不同的反事实现应:存在轴和形式轴

共享检索索引让一条记忆的结局依赖其它记忆的赋值(即 $Y_i(a)=Y_i(a_i,a_{-i})$,SUTVA 无干扰部分被违反)。因此"换个存法的收益"没有唯一答案,它依赖背景。我们不回避,而是把它拆成**两个不同的干预对照**,这正是"两个轴"的数学含义。

### 存在轴(存与不存):$p$ vs $\varnothing$

**在场效应**——"以形式 $p$ 存这条记忆,相对**完全不放进热索引**,给整个系统的查询期望带来多少变化":

$$\Delta^{E}_{p}(t;b)\;=\;\mathbb E_{q\sim\mathcal D_Q}\big[u(q;b[t\!\to\!p])\big]\;-\;\mathbb E_{q\sim\mathcal D_Q}\big[u(q;b[t\!\to\!\varnothing])\big].$$

> **白话**:这条记忆**在不在场**,对部署查询的期望结局影响多大。这是存在轴要回答的问题——"这条记忆本身值不值得占用检索容量"。

### 形式轴(存成什么):$p$ vs $\texttt{raw}$

**表示效应**——"既然已经决定要存,换成形式 $p$ 相比存原文,多带来多少变化":

$$\Delta^{F}_{p}(t;b)\;=\;\mathbb E_{q\sim\mathcal D_Q}\big[u(q;b[t\!\to\!p])\big]\;-\;\mathbb E_{q\sim\mathcal D_Q}\big[u(q;b[t\!\to\!\texttt{raw}])\big].$$

> **白话**:存在轴先决定"这条记忆要不要占用检索容量",形式轴再决定"如果占,以什么形式占"。两者是两个不同的干预对照,分别由 $\Delta^{E}$ 与 $\Delta^{F}$ 度量。

两个效应都是**声明背景 $b$ 下的条件局部量**:本文全部主标签均在 $b^{\text{raw}}$(全原文背景)下测得,背景漂移由 E 族特征条件化与层 2b/3 审计处理(性质三),标签不声称跨背景不变。

两个效应差一个与 $p$ 无关的常数:**恒等式** $\Delta^{E}_{p}=\Delta^{F}_{p}+\Delta^{E}_{\texttt{raw}}$,而 $\Delta^{E}_{\texttt{raw}}$ 不依赖 $p$。因此:

$$\arg\max_p\Delta^{E}_{p}\;=\;\arg\max_p\Delta^{F}_{p}.$$

即**形式轴的排序两边完全一样,只有存在轴的阈值被 $\Delta^{E}_{\texttt{raw}}$ 平移**。这有两个直接后果:(一)形式轴可以用"相对存原文"的 $\Delta^{F}$ 来测,不必额外测"完全不在场"这一组(只有存在轴判"是否存"时那一组 $\texttt{raw}$ 基准需要缺席背景);(二)过去用"最优存法相对原文的增值"当存在轴收益项的做法,正是把"表示效应"误当成了"在场效应",会漏掉"存原文本身就很有价值"的情形——这一节把两条轴彻底分清。

由于背景不同、对照不同,系统里实际存在**三个互不相同的测量量**。把它们分开命名、各司其职,是全方法能自洽的前提——混用任何一个都是统计错误:

**表 3｜三个测量量与两个效应的对应。**("表示效应"= 形式轴,即 $p$ vs $\texttt{raw}$;"在场效应"= 存在轴,即 $p$ vs $\varnothing$。)

| 测量量 | 定义 | 对应效应 | 统计含义 | 用途 |
|---|---|---|---|---|
| **统一粗筛** $\tilde\tau_p(t)$ | $\Delta^{F}_p$ 在"全库统一用 $p$"背景下整体重放 | 形式轴(混入了干扰) | 两个全域策略之差在单条上的投影,不是干净的个体效应 | **只用于剪枝**,不训练、不冒充个体效应 |
| **在场效应** $\Delta^{E}_p(t)$ | $\mathbb E[u(\cdot;b^{\text{raw}}[t\!\to\!p])-u(\cdot;b^{\text{raw}}[t\!\to\!\varnothing])]$ | 存在轴(raw 背景) | $p$ vs $\varnothing$,干净的"存与不存"对照;三个索引状态同测 | **逐臂主训练标签**(含 `raw` 臂自身) |
| **表示效应** $\Delta^{F}_p(t)$ | $\Delta^{E}_p(t)-\Delta^{E}_{\texttt{raw}}(t)$(性质一恒等式) | 形式轴(固定背景的局部对比) | 相对存原文的增值 | 派生量:决策比较与卡价表用 |
| **背景复测** $\Delta^{E}_p(t;b)$ | 上式在 $b\sim\mathcal B$ 下对随机子样本重测(层 2b/层 3) | 两轴(环境审计) | 部署混合背景下的效应样本 | **背景敏感性审计专用**,非训练主干 |

### 把 $\Delta^{E}$ 拆成"相关收益"与"无关干扰"

由于 $\Delta^{E}$ 是在 $\mathcal D_Q$ 上的期望,它可以自然地按"这条查询是否需要 $t$"做全概率分解。记 $\pi_t=\Pr_{q\sim\mathcal D_Q}\big(t\in E(q)\big)$ 为 $t$ 被需要的**倾向度**,令 $\Delta u_p(q)=u(q;b[t\!\to\!p])-u(q;b[t\!\to\!\varnothing])$,则:

$$b^{\mathrm{rel}}_p(t)\;:=\;\mathbb E\big[\Delta u_p\mid t\in E(q)\big],\qquad B_p(t)\;:=\;\pi_t\,b^{\mathrm{rel}}_p(t),$$

$$\Delta^{E}_{p}(t;b)\;=\;\underbrace{\pi_t\,b^{\mathrm{rel}}_p(t)}_{\text{相关收益 }B_p(t)}\;+\;\underbrace{(1-\pi_t)\,\mathbb E[\Delta u_p\mid t\notin E(q)]}_{\text{无关外溢,通常 ≤ 0 但不恒负}}.$$

第二项**通常**为负:一条无关的记忆进场,可能把真正需要的证据从前 $k$ 名挤出去;但它不恒为负——无关记忆偶然改善排序、或提供辅助背景的情形同样会发生。因此我们不把它人为 clamp 成非负,而是定义**有符号外部性**(signed externality)为它的相反数:

$$\Psi_p(t)\;=\;-(1-\pi_t)\,\mathbb E\big[\Delta u_p\mid t\notin E(q)\big]\;\in\;\mathbb R.$$

于是 $\Delta^{E}_{p}=B_p-\Psi_p$:相关收益 $B_p$ 与有符号外部性 $\Psi_p$ 两个名字在全文含义唯一(§4.1.2 的合成估计与此处同名同义)。$\Psi_p>0$ 表示净干扰(harmful interference),$\Psi_p<0$ 表示净增益外溢(beneficial spillover)——恒等式 $\Delta^E_p=B_p-\Psi_p$ 在任意符号下都成立,这是比"非负假设"更干净的记账方式。**注意:这里的 $B_p$ 和 $\Psi_p$ 都已经是"每个查询"的期望单位**,可以直接相减。这正是把相关收益与无关干扰放回同一个期望单位、避免"一个求和一个取平均"量纲错位的关键。

## 3.5 净存储价值与 `none` 的四条硬定义

存在轴的**净存储价值**为:

$$V_{\text{store}}(t)\;=\;\max_{p\in\mathcal P}\Big[\underbrace{\Delta^{E}_{p}(t)}_{=B_p-\Psi_p}\;\; -\;\lambda_w C^{\text{write}}_p(t)\;-\;\lambda_r C^{\text{read}}_p(t)\;-\;\lambda_s C^{\text{index}}_p(t)\Big],\qquad V_{\text{none}}(t)\equiv 0.$$

其中三项成本分别是:一次性构建调用成本 $C^{\text{write}}_p$、该形式在期望上占用的未来读取量 $C^{\text{read}}_p$、以及索引规模成本 $C^{\text{index}}_p$,都用同一个查询分布 $\mathcal D_Q$ 的期望单位衡量,再由各自的系数 $\lambda_w,\lambda_r,\lambda_s$ 折算到收益单位——$\lambda$ 是**显式声明的运营汇率**(多少钱换一分质量),用验证集在前沿上选点并做敏感性报告。量纲上需要一句显式声明:$\Delta^{E}$ 与 $C^{\text{read}}$ 是每查询单位,而 $C^{\text{write}}$ 是一次性支出、$C^{\text{index}}$ 是存量;三者可直接相减的前提是 $\lambda_w,\lambda_s$ 为**复合汇率**——声明摊销基准 $N_Q$(记忆库生命周期内预期服务的查询数,本文取 $5\times10^3$ 量级),$\lambda_w=w/N_Q$、$\lambda_s=s/N_Q$,$\lambda_r$ 直接同单位。测量与标签层只存分量(§4.1.2 第五步):**净值永不入库,汇率变更不需要重新测量**。**注意 $\max_p$ 作用在整项上**——干扰 $\Psi_p$ 和读取成本 $C^{\text{read}}_p$ 都依赖具体形式 $p$($hq$ 卡可能把无关查询也排上来、`raw` 与 `raw+hq` 占的容量不同),不能只对 $\Delta^{E}$ 取最大再把与 $p$ 无关的项减在外面。

> **白话**:以最优形式在场,最多挣多少(在场效应 $\Delta^{E}$,它已经把"这条记忆自己带来的相关收益"和"它挤掉别人带来的干扰"合并成 $\Delta^{E}=B-\Psi$),再减去它占用的读取与索引两类成本。**净账为正才存。** 这里要特别提醒:$\Delta^{E}$ **本身已经包含了干扰成本 $\Psi$**,所以净存储价值里**不应再单独减一次 $\Psi$**,否则就是重复计算——本文在 $V_{\text{store}}$ 中,$\Psi$ 只作为 $\Delta^{E}=B-\Psi$ 的一部分出现,不再单列。$\Psi$ 之所以必须测,是因为检索增强生成领域反复证明"语义相关但不含答案"的干扰段落会显著拉低答案质量 [7,8]——这就是把无关记忆存进库的代价。

`none` 的四条硬定义(不是"剩下的选项",而是被定义的零点、与其余十个动作结构性对立的对照点):

1. **零热索引足迹**(zero hot-index footprint):不入热索引;正常检索路径零足迹、零读取成本、零神经调用——仅在读侧低置信时,才可能经冷归档扩召回被临时取回(§4.5)。别的动作都在"花未来的容量",只有 `none` 在"省未来的容量"——它是全部存法的对偶。
2. **不删除**:原文进冷归档,按 `turn_id` 可随时恢复。判断错了可以纠正。`none` 与"删除"是两回事,全文不把二者挂钩。
3. **保留观测**:以 $\eta=5\%$ 的概率进影子索引,被 `none` 的记忆仍被抽样观测——判断器可以持续修正,曝光偏差可控。
4. **被测量**:$V_{\text{none}}(t)\equiv 0$ 是定义而不是估计;"存"的价值是被测出来的净账。

## 3.6 可观测信息与统计前提

**写侧可观测的信息。** 写时可见信息正式定义为
$$x_t\;=\;\big(\,t,\;\ t_{<t},\;\ \mathcal I^{\mathrm{ref}}_{t^-},\;\ \mathcal I^{\mathrm{hot},\pi}_{t^-}\,\big),$$
共四部分:当前轮次、它之前的轮次、只含更早会话原文形态的**固定参考索引** $\mathcal I^{\mathrm{ref}}_{t^-}$(供 I 族"索引彩排"使用,规则固定、与决策无关),以及写入那一刻热索引的**实际状态** $\mathcal I^{\mathrm{hot},\pi}_{t^-}$(供 E 族"索引状态"使用:库大小、命中密度、重复度、各类卡占比、邻域拥挤度;它由到当前为止的部署序列诱导,不含任何未来信息)。策略必须输出 $a\in\mathcal A$。**禁止进入 $x_t$ 的信息**:当前轮次之后的文本、真实的未来查询、当前会话未来的索引状态。训练时同样禁止,否则是泄漏而不是学习。

**两个统计前提。** 第一,索引可重建意味着潜在结果可观测(full feedback):给定 $T$、$Q$ 与固定的检索配置,对任意写入赋值 $a$,我们都能重建索引 $\mathcal I(a)$、重跑 $Q$、读出每一个动作的结局。因此写入动作的收益不需要估计,可以直接测量——这是潜在结果框架里通常做不到、而这里做得到的事。第二,共享索引意味着 SUTVA 不成立:一条记忆的结局依赖其他记忆的赋值,这使得"个体效应"的定义失效。但我们不回避,而是把三种背景下的三个量分别命名、分别用在各自该用的地方。由此得到一个直接推论——跨记忆的分数来自不同背景,不可比。

---

# 4 方法:R2W

本章按"离线测量、训练、决策配置、在线"四段组织。整体流程:离线阶段(一次性、全信息、零可学参数)依次完成证据反转、可能问题生成、三层测量与分量标签生产,随后训练存在轴判断器(收益 + 不确定度)与形式轴逐臂择形器(pooled GBM),再经普通 validation 确定决策配置(效用汇率、保守系数、保真标准与护栏);部署后每轮依次执行第一关(只看记忆自身特征与廉价索引状态)→ `none` 分支(冷归档加影子索引)或第二关(出题 → 短名单草稿构建 → 契约与证书预检 → 逐臂打分 → 两行决策,落 `raw` 即存原文);查询到达后经引用解析、纠偏、回流、微调、漂移监测形成闭环。逐步骤清单见附录 C。

## 4.1 离线测量:全部零参数、确定性

全程零可学参数:从证据到画像、三种效应、答题分、净价值、证书,全是确定性的测量与计算。产物是标签,不是模型——这是蒸馏范式成立的前提。

### 4.1.0 数据划分与配置冻结

按**对话**切分为 60/20/20(按轮切会让测试题的证据伙伴泄进训练集);从训练区留出 10% 的对话做超参选择($\lambda/\kappa/\theta$ 的 validation)与案例修正拟合;检索器、阅读模型、判分器的配置写成快照冻结。对话级切分给出近似独立的样本划分(普通监督学习的常规前提),并确保"测的就是部署系统"。

### 4.1.1 标签一:未来使用画像 u(t)

benchmark 的 evidence 告诉我们"每个问题需要哪几轮";反转即得"每轮被哪些问题用到" $U(t)$,并把共同被读到的关系连成图(同一个问题的证据伙伴互相连接,超过 12 个伙伴就随机抽 12 个)。画像把"用到这条记忆的问题向量"按相关度加权聚合(而不是简单平均——不同主题的问题会互相抵消),再拼上一阶共同被读到的伙伴信息:

$$u_{\text{set}}(t)=\sum_{q\in U(t)}\alpha_q\, e(q),\quad \alpha_q\propto\exp\!\big(e(q)^\top e(t)/\tau_u\big),\quad
u_{\text{comb}}(t)=\frac{\sum_{v\in N(t)}w_{tv}\, [\,u_{\text{set}}(v)\,\|\,e(v)\,]}{\sum_{v\in N(t)}w_{tv}},\qquad
u(t)=[\,u_{\text{set}}(t)\,\|\,u_{\text{comb}}(t)\,]\in\mathbb R^{3d_e}.$$

其中 $e(\cdot)$ 是检索器的语义向量, $N(t)$ 是 $t$ 的一阶共同被读到的伙伴集合, $w_{tv}$ 是 $t$ 与 $v$ 被同一批查询共同引用的强度, $U(t)=\varnothing$ 时 $u_{\text{set}}(t)=\mathbf 0$。

$u(t)$ 是零参数、确定性的标签,不含任何可学参数——这是蒸馏范式成立的前提。它编码的是"这条记忆的未来使用画像";上线时,打分器必须仅凭内容预测它,这就是特权信息蒸馏:teacher 看得见未来,student 学会不看未来也能猜到。本版中画像蒸馏降级为**可选辅助目标**(表示正则),是否启用由消融决定(§7.4.1)——它不再承担案例库检索键的职责(§4.2.4)。

### 4.1.2 标签二:三层测量、答题与净价值

**生成器预生成可能问题。** 用域内微调的小生成器(T5-base 级别)以贪心解码(确定性,保证标签可复现)为每轮生成 1–2 个"可能被这样问"的问题;再做合格性过滤:生成的问题必须能在这轮正文里找到答案锚点,找不到就丢弃(允许全丢)。对稠密检索,生成的问题必须独立编码为单独的索引单元,不能拼进原文——拼接会稀释原向量。

**层 1 统一粗筛加答题实验。** 每个存法全库重建一次索引(索引卡独立编码、契约内建),跑全部问题,按统一的读出协议记录条级排名与覆盖,得到粗筛分 $\tilde\tau_p(t)$。同场让固定的阅读模型真作答(读 top-k、带当前日期、强制标注引用了哪几条),记录结局分 $u_p$(供粗筛参考)与监控口径分:

$$y_{\text{on}}(t,q)=f\big(\mathrm{rank}(q,t)\big)+0.5\cdot\mathbb 1[t\ \text{被答案引用}].$$

产出每个 $(t,p)$ 的粗筛分与剪枝表。这里 $y_{\text{on}}$ 在离线与在线**逐字同式**:第 4.4 节的引用解析用的就是这一条公式,全文只此一个编号。

这里有一条必须遵守的纪律:$\tilde\tau$ 只用于剪枝(阈值 $\epsilon=-0.05$),绝不进入训练损失。一个常见但危险的简化是把 $\tilde\tau$ 当作"代理效应"直接拿去训练——它会混进干扰、把全域策略差误当成单条因果效应,本文明确反对这种做法。剪枝的召回率与**最优臂被误剪的概率**(层 2 实测的最优动作却被层 1 剪掉的比例)是必须公开报告的诊断量(§7.3.2);它直接决定"筛后"口径的代价。

**层 2a 单条替换:五步成签。** 在"全原文"背景库里,对候选记忆 $t$ 与通过粗筛的存法 $p$ 做"删旧插新"的增量交换(索引单条增删是毫秒级操作),同一次操作中维护三个索引状态——$b[t\!\to\!p]$、$b[t\!\to\!\texttt{raw}]$ 与缺席对照 $b[t\!\to\!\varnothing]$——量完复原。一条 $(t,p)$ 的标签按五步算出:

**第一步(同题配对)。** 对每条查询同题配对算差值 $\Delta u_p(q)=u(q;b[t\!\to\!p])-u(q;b[t\!\to\!\varnothing])$。同题配对消掉题目难度噪声,是整条链里最重要的方差缩减。

**第二步(分层)。** 查询按"是否需要 $t$"分两层:相关层($t\in E(q)$,按证据标注)全跑;无关层抽一组 $Q_\perp(t)$(约占层 2 开销的 10–20%,用于估干扰)。分层是因为 $\pi_t$ 往往很小——不分层会把阅读模型的调用全部烧在零信号查询上。这在统计上就是分层抽样估计同一个期望,不是两个概念。

**第三步(层内聚合 + 收缩)。**

$$b^{\mathrm{rel}}_p=\mathbb E[\Delta u_p\mid t\in E(q)],\qquad \tilde b^{\mathrm{rel}}_p=\frac{n_{rel}}{n_{rel}+n_0}\,b^{\mathrm{rel}}_p+\frac{n_0}{n_{rel}+n_0}\,\bar b_{p\times\text{类型}},$$

即向"存法 × 内容类型"的均值做经验贝叶斯收缩($n_0$ 由验证集定)。用途次数呈长尾,大量被引用轮次只被 1 个问答使用($n_{rel}=1$),不收缩这些标签就是噪声;$n_{rel}\ge2$ 的条目再做**交叉拟合**:一半查询选 argmax、另一半查询估值,消掉"赢家诅咒"。

**第四步(合成在场效应与干扰)。**

$$\pi_t=\Pr_{q\sim\mathcal D_Q}\big(t\in E(q)\big),\qquad B_p=\pi_t\,\tilde b^{\mathrm{rel}}_p,$$

$$\Psi_p=-(1-\pi_t)\,\frac{1}{|Q_\perp(t)|}\sum_{q\in Q_\perp(t)}\Delta u_p(q),\qquad \Delta^{E}_p=B_p-\Psi_p.$$

$\Psi_p$ 有符号、不 clamp(无关记忆偶然帮上忙的情形就记负值,理由见 §3.4);乘 $(1-\pi_t)$ 使干扰与相关收益同处"每查询"期望单位——这是 §3.3 量纲纪律的关键,也是 $V_{\text{store}}=\max_p[\Delta^{E}_p-(\lambda_w C^{\text{write}}_p+\lambda_r C^{\text{read}}_p+\lambda_s C^{\text{index}}_p)]$ 各项能直接相减的原因。三个状态同测,顺带产出表示效应 $\Delta^{F}_p=\Delta^{E}_p-\Delta^{E}_{\texttt{raw}}$(性质一恒等式,§5.1)。

**第五步(成本计数与分量入库)。** 读取与索引成本依形式而异,但两类**严格分开**:记 $L^{\text{ctx}}_p(t)$ 为真正送进阅读模型上下文的正文 token 数(索引卡按契约不进上下文,见表 2),$\hat\rho_p(t)=\mathbb E_{q}[\mathbb 1[t\!\in\!\text{top-}k(q;\cdot)]]$ 为该形式下的命中率,则

$$C^{\text{read}}_p(t)=L^{\text{ctx}}_p(t)\cdot\hat\rho_p(t),\qquad C^{\text{index}}_p(t)=\text{索引卡与存储单元规模},\qquad C^{\text{write}}_p(t)=\text{一次性构建量}.$$

全部用契约截断后的真实分词计数(公共块两边等量、差分自动抵消),且全流程——成本预测、契约执行、$\lambda$ 标定——共用同一个计数器:部署阅读模型的分词器。由于卡不进上下文,同一 $\hat\rho$ 下 $C^{\text{read}}_{\texttt{raw+X}}\equiv C^{\text{read}}_{\texttt{raw}}$,索引卡的开销只计入 $C^{\text{index}}$。**标签按分量行存储(9 列)**:$(t,p):(\Delta\hat E_p,\ C^{\text{write}}_p,C^{\text{read}}_p,C^{\text{index}}_p,\ \text{mask},\ \text{SE},\ n_{rel},\ n_\perp,\ \hat\rho_p)$——**净值永不入库**:净存储价值在训练与决策时才按声明的 $\lambda$ 合成,汇率调整不需要重新测量。新增的后三列各有用途:$\text{SE}$ 供主动采样与训练加权使用;$n_{rel},n_\perp$ 使收缩强度可复查;$\hat\rho_p$(该形式下进 top-$k$ 的命中率)只花检索、不花 reader 调用即测出,是第二关 $\hat\rho$ 学习目标的稠密监督来源(§4.2.3)。最后做**主动采样**:优先测"两种最优存法差距小"的边界记忆——只有它们的决策会改变最终的遗憾(margin 论证)。

> **一个走账小例子(示意数字,非实验结果)。** 记忆 $t$="Caroline 5 月 4 日搬去墨尔本"(正文 210 token,事件/时间线类型):重放 400 条查询,12 条需要它($\pi_t=0.030$),另抽 60 条无关查询;粗筛存活 `raw`、`raw+event`、`sum` 三臂。以 `raw+event` 走完五步:相关层 12 条配对差值中,选臂半区均值 $+0.500$、估值半区 $+0.420$(低于选臂半区——赢家诅咒的实证形态,cross-fit 取后者),向(event×时间线)先验 $0.35$ 收缩($n_0=4$)得 $\tilde b=0.392$;无关层均值 $-0.008$。故 $B=0.030\times0.392=0.0118$,$\Psi=0.970\times0.008=0.0078$,$\Delta\hat E_{\texttt{event}}=+0.0040$,$\text{SE}\approx0.0040$——信噪比约为 1,单条记忆的标签本就是小信号,这正是收缩、配对、主动采样与决策端保守项存在的原因。成本(声明汇率 $\lambda_w=2\times10^{-6}=w/N_Q$、$\lambda_r=2\times10^{-5}$、$\lambda_s=5\times10^{-7}$):构建 380 token,$C^{\text{read}}=L^{\text{ctx}}\cdot\hat\rho=210\times0.42=88.2$(表 2:卡不进上下文,但正文照常被读),索引单元 295,折算合计 $0.0027$,净值 $+0.0013$。同一过场里 `raw` 的相关收益更低(收缩后 $+0.337$)、无关干扰更大($-0.012$),$\Delta\hat E_{\texttt{raw}}\approx-0.0015$——原文在这条邻居手里是净负资产;`sum` 压缩后 $\Delta\hat E=+0.0030$、成本最省,净值 $+0.0016$。三臂并排立刻给出两个教训:其一,收益最优臂(`event`)与净值最优臂(`sum`)可以不一致——$\max$ 必须作用在整项上,不能先对 $\Delta^{E}$ 取最大再把成本减在外面(附录 A13 的臂一致率因此公开报告);其二,$\lambda_r$ 降为零时最优臂翻转为 `event`(净值 $+0.0031$ 对 $+0.0024$)——净值不入库、分量入库,换汇率只是重算一次算术。`none` 与其余十个动作**结构性对立**,由此不是口号而是账。这一行行的 $(\Delta\hat E,C^{\text{write}},C^{\text{read}},C^{\text{index}},\text{SE},n_{rel},n_\perp,\hat\rho)$ 就是入库的分量标签。

**层 2b 多背景复测(环境审计,非训练主干)。** 层 2a 的主标签在"全原文"单一背景下测得,上线却是混合背景,不同背景可能让排序反转。本版不再构建多背景训练标签,而是对随机抽取的 10–20% 样本,从一组背景 $b\sim\mathcal B$(全原文、随机混合、当前策略背景)中抽取若干 $b$,做与层 2a 相同的单条替换(含缺席对照),产出背景条件量 $\Delta^{E}_p(t;b)$ 与 $V_{\text{store}}(t;b)$。它们的用途只有两个:①**背景敏感性审计**——raw 背景与策略背景下的排序 Kendall-$\tau$ 与决策一致率(§7.1.1 前提验证 3);② **E 族特征价值的判据**(§7.0 ⑥)。判读规则写死:若审计发现排序普遍反转,则如实把"单背景标签"降级为近似、把多背景训练列为必要修正重测——不许事后把审计说成没问题。

**层 3 策略背景一次性复测(审计)。** 用层 2 训出的初始策略 $\pi_0$ 把留出对话混合存一遍,对留出集做同样的单条替换(含缺席对照),得到策略背景标签与答题分。它只回答一个问题:"在真正部署的混合环境里,raw 背景标签的排序与决策还成立吗"——一次性、不迭代;报告两轮背景差异与排序一致性,若不稳定则只作实现启发式表述,不作为结论引用。



### 4.1.3 标签三:事实保真证书

抽取类存法专用的保真证书,零可学参数:把抽取结果拆成一条条原子事实,然后用 NLI 蕴含比对逐条与原文比较(FActScore 范式 [61] + AlignScore [62]),得到证书分——覆盖率加权减去矛盾数加权再加置信边际加权,权重在人审子集上拟合。已知盲区是:逐条比对抓不住"把真话拼成假话"的组合骗术 [63]——因此冷归档原文永远可查,并定期人工抽查。该证书同时用于**在线草稿预检**(§4.4):构建产物先持证、再参与择优——被否决的草稿没有机会上线,"构建后验收再回退"的旧结构随之取消。

三层测量各自对应一种背景、一类产物、一种用途,对应关系已在表 3 固定,逐步骤清单见附录 C。

### 4.1.4 大模型与小模型各用在哪(边界写死)

为避免"哪里烧了大模型、省了没有"口径不清,本文把全部模型调用按类登记。原则只有一条:**大模型只用于测量被测系统(答题与判分),决策与构建全部用小模型。**

| 部位 | 用什么模型 | 是否大模型 | 调用时机 | 计费口径 |
|---|---|---|---|---|
| 反事实测量的答题(reader) | 部署阅读模型(版本、温度、提示冻结) | **是** | 仅离线:层 1 全量、层 2/3 局部 | 被测系统自身消耗,摊销见 §7.4.4 |
| 测量评分(judge) | 固定语言模型判分器,仅开放类、成对差分模式(人判锚定抽检,§6.2) | **是** | 仅离线测量评分 | 同上,一次性 |
| 事实保真证书 | NLI 蕴含模型(AlignScore 级) | 否 | 离线出证书;在线验收与回退判定 | 小模型 |
| 可能问题生成 + 索引卡构建 | 域内微调 220M 级生成器,贪心解码 | 否 | 离线全量;在线只对放行的约 24% 轮次出题并构建 2–3 个短名单草稿 | 构建调用,全链路唯一重开销 |
| 存在轴 / 形式轴预测器 | GBM(具体 boosting 实现由验证集确定;岭回归 / 案例回归 / ≤12M 网络为审计基线,§4.2.3) | 否 | 在线每轮一次前向(第二关仅放行轮),轻量级 | 决策路径零大模型调用 |

由此,反事实重放"真跑下游"所调用的大模型,是**被测系统本来就有的 reader 与 judge**——R2W 不在其上叠加任何大模型 teacher;而在线写入决策路径(第一关 → 逐臂打分 → 两行标量比较)不调用大模型。这一区分也回答了"为什么不用大模型直接做写入判断":R2W 刻意避免依赖大模型的即时 teacher 判断,使决策规则更便宜、更固定,并为下游的风险分析、复核与审计提供显式标量接口(§2.2)。


## 4.2 模型与训练

### 4.2.1 级联两关:先判值不值得存,再挑存成什么

每条新记忆先进**第一关 = 存在轴**(便宜,所有记忆都过,零生成调用)。它看两类信息:一是记忆自身的特征(M 族,§4.2.3),二是**一次 ANN 查询就能拿到的、描述"当前库里这条记忆会不会被挤/会不会挤别人"的廉价索引状态**(I/E 族的廉价子集:当前库大小、与已有记忆的 top-1/top-k 相似度、局部密度、重复度分数、邻域 margin、同主题已有记忆条数)。这些都不用生成模型。之所以必须让**第一关**就看到、而不是推迟到第二关,是因为"这条记忆值不值得存在"恰恰最依赖冗余度和当前索引状态——库里已经有一条几乎等价的记忆,它的边际价值就可能近于零甚至为负,这正是存在轴要判断的事。判断器输出**收益包络与不确定度两个标量** $(\hat y_b,\hat\sigma)$,成本不学、由确定性计算器 $\bar C(x;\lambda)$ 合成(§4.2.2):净账 $\hat y_b-\bar C$ 加保守项 $\kappa_E\hat\sigma$ 不大于零,就判 `none`(零热索引足迹、冷归档、5% 影子索引,零 AI 调用)——零点不是学出来的阈值,而是"净账不赚就不存"这一定义直接给出的决策边界。约 76% 的轮次在此零调用终止。通过者(约 24%)进**第二关 = 形式轴**:先做两件离线很便宜的事——220M 生成器出题、对 2–3 个短名单候选做确定性**草稿构建**(产物可被精确测量、可先验证书)——再由**逐臂打分的 pooled GBM**在"原文 + 持证草稿"上选净值最高者(§4.2.3、§4.3)。

**两关训练的公共纪律。** ①第二关训练集 = 全部有反事实标签的记忆,不做人为 gate 筛选——$\Delta^E_p$ 是条件结局模型,即便最终判 `none` 的记忆,其各存法效应照样可测可学,做入选筛选只会引入新的选择性偏差;②"该不该存"的监督口径是 $V_{\text{store}}>0$(§3.5),不是 $S(t)=1$——被 benchmark 引用只是证据层面的观测,与"值不值得存在"不是同一个变量;③不声称训练分布等于上线分布:上线第二关的输入分布由学到的存在门诱导,我们不假装能在训练时精确匹配它,未筛选的全体有标签样本是无人为选择偏差的直选方案;④未测到的存法格用掩码跳过,测到了就学它真实的效应。

### 4.2.2 第一关判断器:学"最优臂的收益"一个标量,成本走确定性计算器

第一关不看每个臂的细节,要直接估出"这条记忆最多值多少"。§4.1.2 的分量标签给它**一个**直连的训练目标:

$$\hat y_b(x)\approx Y_b(t)=\max_{p\in\mathcal P}\Delta^{E}_p(t),$$

标签 $Y_b$ 已在测量端做 cross-fit(选臂半区与估值半区分离,§4.1.2 第三步),$\max$ 作用在带噪估计上的向上偏差不会带进训练。成本不进学习目标,由**确定性计算器**合成:

$$\bar C(x;\lambda)=\lambda_w\,\bar W_{\text{类型}}+\lambda_r\,L^{\text{ctx}}_{\texttt{raw}}(x)\cdot\bar\rho_{\text{类型}}+\lambda_s\,\bar I(x),$$

其中 $L^{\text{ctx}}_{\texttt{raw}}(x)$ 是当前轮真实 token 数(写时本就可观测),$\bar W_{\text{类型}},\bar\rho_{\text{类型}}$ 是训练集冻结的"存法×内容类型"总体均值(总体量,合法先验,部署用冻结查表),$\bar I(x)$ 是契约上限下的索引足迹。四点说明。**其一,为什么成本不学**:构建与索引成本受契约上界约束、方差极小;读取成本里唯一真正需要预测的量是命中率,第一关用类型均值已够——第一关是粗筛不是终审,它的正确性标准是方向对且便宜。**其二,汇率彻底移出模型**:若把 $\lambda$ 折算过的成本作为学习目标,"换汇率不换模型"即告失效;现在换汇率只需重算 $\bar C$ 的一次算术,预测器纹丝不动(与"标签分量入库"是同一条纪律)。**其三,收益最优臂与净值最优臂不一致(成本倒挂)时**,$\hat y_b-\bar C$ 是净值包络的一阶近似:判决方向由验证集的"放行率对齐"诊断校验(实测 $V_{\text{store}}>0$ 的轮次占比),附录 A13 的臂一致率相应降级为纯诊断量、仍公开报告。若诊断显示一阶近似不足,退路是第一关也做逐臂预测再取 max——更准但更贵,列为备选而非默认。**其四,两类 gate 错误都有兜底**:错放行只花一次第二关构建调用(约 24% 放行率有先验记账);错拦截由 $+\kappa_E\hat\sigma$(刻意往存的方向偏)与 5% 影子索引、冷归档扩召回(§4.5)兜底。$\hat\sigma$ 的来源与第一关的具体实现见 §4.2.3。

**要特别注意的选择偏差**是:不能把"没被 benchmark 引用"当成确定的"不需要"。引用只是真实未来使用的一个有偏抽样——没被引用不等于没用。判断器的训练信号全部来自 §4.1.2 测出的 $\Delta^{E}_p$、$\Psi_p$ 与三类成本——**测出来的、确定性的量**,而不是从"是否被引用"这个有偏标签推断出来的。$\Delta^{E}$ 的定义本身依赖"这条记忆未来会被问到",而 benchmark 的 evidence 只覆盖它选的那一组问题;我们用"相关/无关查询分层抽样 + $\pi_t$ 倾向度"分开估计(§4.1.2 第二步),避免把"没被标注"当成"确定无关"。若仍想进一步纠正"benchmark 问题太稀疏"的偏差,可以在附录用 PU/SAR 做扩展(在某些强近似下,用未被引用的样本当弱负例);但**不放主方法**——它依赖的 SCAR 假设在人工构造的查询分布上往往不成立,主方法只依赖站得住的假设。

### 4.2.3 第二关择形器:逐臂打分的 pooled GBM

写入时未来的查询不存在。检索效果由"文档、查询、匹配"三要素决定(LETOR 经典划分 [57]),因此 R2W 用四族特征把后两族补上,第四族刻画"上线时索引里有多少邻居在跟这条记忆抢位置":

| 族 | 维度 | 内容 |
|---|---|---|
| **M 记忆自身** | $d_e+33$ | 部署检索器自己的编码器出的语义向量(复用索引构建,零成本、同一几何空间);BM25 统计量 15 维(打分公式里的量,跨语料可迁移);结构统计 13 维;位置 5 维 |
| **Q 可能提问** | $d_e+12$ | 生成的问题的向量与类型统计(问多少钱?问什么时候?——几乎直接指出该配哪种索引卡,完全不用未来信息) |
| **I 索引彩排** | 18 | 把这条记忆当查询去参考索引搜一次(看邻居竞争激不激烈);把生成的问题当查询再搜一次(彩排"将来能不能被搜到") |
| **E 索引状态** | $d_e{+}10$ | 当前(写时)热索引的全局与局部状态:$\lvert\mathcal I\rvert$、命中分数密度、top-$k$ 边缘、重复度高不高、各类别索引卡占比、这条记忆所在邻域的拥挤度——全部是写时已可观测、不构成未来泄漏 |

**两关各吃什么。** 第一关只吃 M 族 + I/E 族廉价子集(嵌入复用索引构建 + 一次 ANN 探针,零生成调用);第二关追加 Q 族(生成问题的向量与问型统计)、完整 I 族(索引彩排)、完整 E 族、**arm 特征**(存法类别、压缩比、卡类型)与**草稿精确统计**(真实长度、token 计数与证书预检结果)。草稿的价值在于:把"预测产物性质"变成"测量产物性质"——成本特征精确、证书可预检,"上线了再回退"的窗口随之消失。**一鱼两吃**:同一份生成的问题既当 `hq` 卡素材,又当 Q 族特征来源,多出的成本几乎为零。参考索引(I 族)固定为"本会话之前所有轮的原文形态",规则固定、与决策无关,训练与上线都可复现;E 族则随上线索引的真实状态变化。

**模型:pooled GBM 逐臂打分(S-learner 范式 [76])。** 对每条训练记忆,通过反事实重放得到各存法的实测效应与命中率;训练时把存法作为 arm 特征与写时可见状态一起输入。同一个 pooled GBM 在测试时对每个候选存法分别打分:

$$\Delta\hat E_p=f_E^{\mathrm{GBM}}\big(x_t,\mathrm{arm}(p)\big),\qquad \hat\rho_p=f_{\rho}^{\mathrm{GBM}}\big(x_t,\mathrm{arm}(p)\big),\qquad p\in\mathcal P.$$

不确定度 $\hat\sigma_p$ 由折间/种子集成离散度或分位数预测区间得到。构建成本 $C^{\text{write}}_p$ 与索引成本 $C^{\text{index}}_p$ 由已经构建的候选草稿直接计数,不再让模型预测;读取成本只预测未来命中率这一不可直接知道的部分,即 $\hat C^{\text{read}}_p=L^{\text{ctx}}_p\hat\rho_p$。因此 test memory 到来时,系统对每个候选 action 分别经过 GBM 得到预测 value,再与成本合成净值后取 argmax。相比"把候选结果压成一个 argmax 类别标签"(FluxMem 式 [27]),该结构保留了逐臂幅度;**`raw` 是普通的第十一个臂**,与加工臂同台比较。

**模型族定位。** GBM 是主线 estimator,具体 boosting 实现由验证集上的期望遗憾、条内排序准确率与校准表现确定;岭回归、纯案例/邻居回归与可选小型网络作为审计基线。若更简单的 estimator 稳定打平或胜过 GBM,则按预注册规则采用更简单者,不把具体 boosting 实现写成方法贡献。

**训练纪律**:①效应模型只在测过的 $(t,p)$ 格子上训练,未测格用 mask 跳过;命中率模型用层 1/2 已记录的 $\rho_p$ 作为稠密监督。②模型选择以**决策指标**为主:按对话分组交叉验证,报告期望遗憾与同一条记忆内部的 arm 排序准确率;跨记忆的效应绝对值不直接作排序比较。③**校准必报**——决策对着零点比,因此留出对话上报告 effect/rho 的校准 slope/intercept,必要时做 isotonic 修正。④不确定度只用最简方案,二选一:K=5 折/种子集成的离散度,或分位数回归($\tau=0.1/0.5/0.9$)区间宽度;到最近训练案例的余弦距离可作为普通输入特征。辅助任务(未来使用画像 $u(t)$ 的蒸馏,§4.1.1)保留为可选表示正则,是否启用由消融决定(§7.4.1)。

### 4.2.4 非参数基线与局部特征:案例库归位

kNN 式案例思想不再充当核心修正器,归位到两个更朴素的位置。**其一,特征**:到最近训练案例的余弦距离、近邻平均实测效应,作为 pooled 模型的普通输入维——局部性交给模型自己去用,不再外挂修正公式。**其二,基线**:纯案例回归(按嵌入检索 K=10 近邻、对同样测过该臂的案例按相似度加权平均其实测效应)进入 §6.3 基线表与 §7.4.1 账本——如果它打平主模型,说明主模型的复杂度没挣到钱,按判读规则收缩结论。**冻结纪律保留**:在线新观测一律只进 staging buffer,攒批后在离线副本重测转正,与下一版预测器同版本原子切换(附录 E);不允许在线直接注入——注入即改变最终动作,线上实际执行的系统就与离线验证过的系统不再是同一个。

## 4.3 决策规则与安全回退

经过 §4.1–4.2,决策需要的全部量已经到齐:第一关的 $(\hat y_b,\hat\sigma)$ 与确定性成本计算器 $\bar C$,第二关的逐臂 $(\Delta\hat E_p,\hat\rho_p,\hat\sigma_p)$、精确计数的构建/索引成本与正文长度,草稿的契约与证书预检结果,以及声明的汇率 $(\lambda_w,\lambda_r,\lambda_s)$ 与保守系数 $(\kappa_E,\kappa_F)$。整个决策只有两行。

**第一行(存在轴)**——净账加保守项不为正,就不占容量:

$$\boxed{\;a_t=\texttt{none},\qquad\text{若 }\ \ \hat y_b(t)-\bar C(x_t;\lambda)+\kappa_E\,\hat\sigma(t)\;\le\;0.\;}$$

其中 $\bar C(x_t;\lambda)$ 是 §4.2.2 的确定性成本近似,由当前可观测长度、冻结的类型均值与声明汇率合成,不由模型学习。零点由定义给出:不存不花钱,净账不赚就不存。$\kappa_E\ge0$ 取**正号**是刻意的——存在轴"没把握就存",与形式轴镜像(理由见 §4.5 的 DUM)。判 `none` 的记忆原文进冷归档、保留 5% 影子索引,不是删除(§3.5)。

**第二行(形式轴)**——在"原文 + 持证草稿"里取预测净值最高者:

$$\boxed{\;a_t=p^*=\arg\max_{p\in\mathcal F_t}\;\Big[\,\Delta\hat E_p(t)-\lambda_w C^{\text{write}}_p(t)-\lambda_r L^{\text{ctx}}_p(t)\hat\rho_p(t)-\lambda_s C^{\text{index}}_p(t)-\kappa_F\,\hat\sigma_p(t)\,\Big],\qquad p^*=\texttt{raw}\ \text{即存原文}.\;}$$

可行集 $\mathcal F_t=\{\texttt{raw}\}\cup\{p:\text{草稿构建成功}\ \wedge\ contract(p)=1\ \wedge\ F_p\ge\theta_{\text{fid}}\}$,其中 $\theta_{\text{fid}}=0.95$ 为预置工程标准(§4.1.3、§6.4)。两点说明。**其一,原文是永远的对手**:`raw` 作为普通臂有真实标签与预测,任何加工臂必须**净挣过原文**才轮得到上场——argmax 落在 `raw`(含草稿全部被否决的退化情形),决策就是存原文。这与"形式轴以存原文为零增量基准"的旧表述数学等价(性质一恒等式 $\Delta^E_p=\Delta^F_p+\Delta^E_{\texttt{raw}}$,排序两边相同),但不再需要任何特设条款。**其二,证书是门禁而不是补救**:草稿**先持证、后上场**——被契约或证书否决的草稿直接不进可行集并记日志,不存在"上线了再回退"的窗口。

> **白话**:整个在线决策就是两句话——"这条记忆的预测净账(拿不准就往存的方向让一点)是不是正的?"以及"在原文和所有合格草稿里,谁的预测净账最高?"没有任何需要校准的统计门槛,没有隐藏的第三个开关。

**超参性质。** $\lambda_w,\lambda_r,\lambda_s,\kappa_E,\kappa_F,\theta_{\text{fid}},\epsilon_0$ 全部是普通系统超参,用普通 validation 选择并登记(附录 C/D)。汇率 $\lambda$ 变更不触发重测量(标签分量入库,§4.1.2),也不触发重训(效应/命中率预测与成本计数分离,§4.2.2–4.2.3)——只重新合成。

**为什么不引入需要校准的门槛层。** 我们评估过把风险校准家族搬到写侧门槛的方案——CRC [46]、分裂/分层 conformal [47]、选择性的 SCRC [48]、漂移下的 ACI [49]——最终放弃,理由两条:其一,被校准的规则难以与真实执行的规则同一(校准所假设的输入分布会被上一版策略本身改写);其二,背景一漂移就要持续重校准,运维复杂度高于换来的保证价值。本结构的一致性来自定义(零点 + 显式保守项 + 硬检查),不声称任何分布无关的错误率上界——这是工程取舍,如实声明。

## 4.4 在线决策与闭环

**写入流程**:第一关特征(M 族嵌入复用索引构建 + 一次 ANN 探针取廉价 I/E 状态,零生成调用)→ 第一行判决:不大于零走 `none` 分支(不入热索引,原文进冷归档,影子索引 5%;**none 不等于删除**)→ 放行轮:220M 生成器出题 + 2–3 个短名单候选**草稿构建**(贪心解码、确定性、本地小模型)→ 契约与 NLI 证书**预检**(不合格草稿弃置记日志)→ 第二关 pooled 模型逐臂打分(四族特征 + arm 特征 + 草稿精确统计)→ 第二行 argmax(落在 `raw` 即存原文)→ 执行(草稿即产物,无二次构建)。每个决策附带决策附件(预测收益/成本/不确定度、被否决草稿及原因、特征与模型版本哈希),可审计。

**`none` 与 `raw` 的分工**:`none` 是"这一条根本不进热索引":省容量、省读取,代价是检索期内不可见(冷归档仍可被读侧扩召回取回,§4.5)。`raw` 是"占一个位置、表示零改动"的普通基准臂:信息完整、无需构建——**但不是无风险动作**:它同样占容量、同样可能挤掉别人,只是每场比较里永远在场的对手。存在轴和形式轴各与各的对照比较,互不越位。

**在线闭环**:**解析引用**得 $y_{\text{on}}$(与离线逐字同式,这是纠偏可行的前提)。**探索**:5% 的动作在可行集内随机选(自己的策略、随机概率已知,属随机实验而非观察数据),判 `none` 的记忆也按 5% 进影子索引继续被观测。**影子对照**:非原文记忆留原文影子副本,同题配对补跑"若存原文",大幅消掉题目难度噪声。**纠偏口径(如实写清)**:在记录完整决策前索引状态、探索概率已知且 positivity 成立的前提下,随机探索允许估计**给定决策前状态的局部单步动作结局**;DR 在结局模型或倾向模型其一正确时具有双鲁棒性,自归一化(SNIPS)以小的有限样本偏差换取更低方差。**本文不据此声称识别跨时间的长期策略价值**——今天的写入会改变明天的索引状态,长期记忆策略的价值识别是另一个问题,不在本方法的承诺之内。**回流**:纠偏与配对重放所得观测一律只进 staging buffer,不进活跃训练集;攒批后在离线副本重测转正,与周期性重训的预测器同版本原子切换(附录 E)。**微调**:小步更新头部、编码器冻结,四个指标(期望遗憾、排序准确率、校准误差、决策一致率)任一恶化即回滚。**漂移监测**:放行率、草稿否决率、残差分布进指标看板,触发人工重新评估;决策参数 $(\lambda,\kappa,\theta)$ 只有重新走 validation 才会变更,变更即登记为新版本配置。

干扰在在线侧分三层处理:离线主标签用单背景(层 2a)、预测模型显式含挤位环境特征(E 族)、残余由多背景审计与随机混存审计量化(层 2b/层 3、§7.1.1)——本文不把长期信用分配当作已解决的模块(见上文的纠偏口径),也不用组合 bandit 与因果图。

## 4.5 对未来不确定性需求的兜底

MemSIF 定义的 **DUM**(写入时显著性预测不了未来用途)是写入侧方法的根本挑战:你永远无法保证"现在看起来不重要的东西,将来一定不重要"。任何纯粹的事前预测都有信息论上的天花板。本文不回避这个问题,而是把 DUM 的处理做成"事前预测 + 事后兜底"的完整覆盖,共三件套:

1. **存在门判决取 $\hat y_b-\bar C(x;\lambda) + \kappa_E\,\hat\sigma$**。形式轴是"没把握就原文"(净值里用 $-\kappa_F\hat\sigma_p$ 惩罚);存在轴与之镜像——"没把握就存"(用 $+\kappa_E\hat\sigma$ 放行,$\hat\sigma$ 取 K 折集成离散度或分位数区间宽度,见 §4.2.3)。也就是说,当判断器对"这条记忆其实有用"信心不足时,我们倾向于不把它拒之门外,而是放进热索引。这与形式轴"没把握就回退原文(信息完整)"是同一个原则的两面:不确定就往保留方向偏,而不是往丢弃方向偏。
2. **高不确定组的保守系数上调**。对 $\hat\sigma$ 高于阈值的记忆组,把 $\kappa_E$ 单独上调(分组普通配置、验证集选)——陌生样本天然被 $+\kappa_E\hat\sigma$ 推向 store。这是分组保守策略,不声称分组统计保证。
3. **读侧低置信时向冷归档扩召回**。当一次查询的读侧分数低(检索自信不足)时,向冷归档扩召回,把"当年判 none、但如今被问到"的记忆重新捞回来。这一步正是 MemSIF 式事后追认的机制——它被吸收进我们的读侧兜底,与写侧的事前预测对接,形成"事前预测 + 事后兜底"的全覆盖。

三件套的分工细节见附录 F。**立场**:真正不可预测的用途(外生随机需求)是写入侧方法的信息论上界,我们靠冷归档读侧扩召回缓解而非消除——边界在 §8.1 展开。

---

# 5 方法性质、假设与失败边界

本章不列"保证清单"——R2W 的决策一致性来自定义而不是校准。本章如实分清三件事:哪些是数学性质,哪些依赖假设,哪些情形方法会失败。

## 5.1 三条方法性质

**性质一(双轴恒等式)。** $\Delta^{E}_{p}=\Delta^{F}_{p}+\Delta^{E}_{\texttt{raw}}$(§3.4)。由此 $\arg\max_p\Delta^{E}_{p}=\arg\max_p\Delta^{F}_{p}$:形式轴的排序与以谁为对照无关,只在存在轴比较时多一个与 $p$ 无关的常数。这是两个轴能共用同一套测量而不互相污染的数学基础,也是"存在轴单测缺席对照、形式轴相对 `raw` 测"这一实验经济性的来源。

**性质二(真值可知时的局部最优)。** 若测得的净价值与效应是精确的,则存在决策 $\text{store}\iff V_{\text{store}}>0$ 与形式决策 $p^*=\arg\max_p[\Delta^{E}_{p}-C_p]$ 正是固定背景、逐条贪心下的局部最优决策(由性质一恒等式,排序等价于"相对原文的增值"比较)。R2W 用"预测值 + 保守项"近似它:预测越准决策越优;预测误差大时 $\kappa\hat\sigma$ 主导,行为退化为"多存原文、边缘样本倾向存"——退化方向是刻意设计成保守的,而不是激进的错误切换。

**性质三(一切结论都是背景条件的局部对比)。** 每个效应都写成 $\Delta_p(t;b)$,只对固定背景 $b$ 成立。本文不声称全局策略最优,也不声称跨背景不变。背景敏感性由三件事处理:E 族索引状态特征的条件化、层 2b/层 3 的多背景审计(§4.1.2)、以及经验诊断(§7.1.1 前提验证 3 的 Kendall-$\tau$)。

## 5.2 成立依赖的假设

完整清单与违反后果见附录 A,此处只点最关键的:A1 测量配置 = 部署配置;A2 训练与部署分布相近(普通监督学习前提,违之则周期性重训);A10 部署查询分布 $\mathcal D_Q$ 由 benchmark 查询加抽样无关查询近似;A12 $\Psi$ 与 $\pi_t$ 的抽样无大偏(以敏感性网格诊断)。这些都**只能被检验、不能被证明,因此一律写作假设而非保证**。

## 5.3 失败边界(R2W 何时会给出坏决策)

逐条列出,并给出对应的缓解或诊断:

1. **查询分布失配**:$V$ 标签整体偏移 ⇒ 用 $\pi_t,\Psi\pm50\%$ 敏感性网格与跨域复测诊断(§7.3.2)。
2. **背景剧烈漂移**(邻域全部换代):E 族外推失效 ⇒ 层 3 一次性策略背景审计 + 漂移看板与周期性重测(§4.4)。
3. **预测误差大**:保守项主导 ⇒ 行为退化为"多 `raw`、边缘样本存",错的是"浪费一点容量"而非"激进改坏表示"。
4. **外生随机未来需求(DUM)**:信息论上界,事前无解 ⇒ §4.5 三件套缓解、不消除。
5. **跨时间步动作**(更新/合并/删除/巩固):局部 per-decision full feedback 失效 ⇒ 超出本方法范围(§8.1)。
6. **无 evidence 标注的日志**:标签需自举 ⇒ 附录 H 给出三种自动 evidence 来源的退化实验。

最后纠正一个容易写错的表述:索引可重建意味着任意动作**都能重放**;但引入跨时间步动作后,问题不再退化为一串彼此独立的监督决策——**不是"不能重放",而是"重放了也降不了维"**。这是 can replay 与 replay 能等价于独立监督决策的区别,前者是 R2W 的真实边界。
# 6 实验设置

## 6.1 数据

| 数据集 | 用途 | 会话数 | 轮次 | 查询 | 备注 |
|---|---|---|---|---|---:|
| LoCoMo | 主实验(对话级 60/20/20) | 10 | 5,882 | 1,986 | 平均每段约 300 轮 / 200 题;五类查询 |
| LongMemEval(-S) | 辅测(跨域) | 500 | 待核实 | 500 | 每题一条独立长历史;每类查询单独计 |
| MSC(附) | 附录补充(结论稳健性) | 待核实 | 待核实 | 待核实 | 用于结论稳健性;构成与引用出处随数据核查登记,见附录 G |

LoCoMo 采用**嵌套(嵌套)划分**:外层是对话级五折,每折 2 个测试对话、其余用于训练与操作点选择;内层对每个折再按对话级 60/20 切出训练区与验证区(从训练区再留 10% 对话做超参选择)。这样"外层五折 + 内层 60/20"的嵌套结构一次说清,避免"一边写五折、一边写 60/20/20"的歧义。报告五折均值与对话级 bootstrap 区间。固定种子 0 下,被 evidence 引用的轮次共 1,430(24.31%),未标注轮次 4,452(75.69%)。用途次数呈长尾:676 个被引用轮次仅被 1 个问答使用。需要区分的是,24.31% 是数据常数(判断器与被引用完全对齐时的放行率),而存在轴放行率是策略行为,两者仅在判断器完美对齐引用时相等。

## 6.2 评测协议

所有对比系统共用同一套 matched harness:同一个阅读模型、同一个判分模型(版本、温度、提示全冻结,判分与人判一致率抽检不低于 90%);同一个 embedding 与 BM25 参数、同一个 RRF 常数、同一个 top-k。指标三轨:主指标为官方 token-F1(Porter 词干);副指标为严格匹配(相对时间题单独解读);**LLM-judge 只用于金答案不可判定的开放类,且一律用成对差分模式**——同一题并排呈现 with/without 两个答案、位置互换去偏、温度 0 判胜负 [77],不输出绝对分。judge 冻结版本、与阅读模型不同族;n≈200 的分层人判锚定子集报告一致率;judge 不参与任何配置选择(validation 与 test 用同一指标,配置先冻结后评)。离线价值标签的结局 $u$ 用同一评分栈(§4.1.2)。分五类报告(单跳、多跳、时间、开放、对抗),对抗题加报拒答率。置信区间一律用对话级 bootstrap(LoCoMo 只有 10 段对话,区间宽是事实,如实报)。每次同报两条参考线:oracle 检索上界与无记忆下界。另外必须换读取策略(单轮、多跳、agentic)复测,以证明收益来自写入侧而非读取侧。

## 6.3 基线

基线优先级排序,以"能否直接检验本文 novelty"为准,而非"覆盖多少系统"。决定本文观点的是下面这几条,而不是一味堆更多的记忆系统:

| 类别 | 系统 | 说明 | 主表 |
|---|---|---|---|
| 写入下界 | all-raw | 全量存原文,无准入 | 是 |
| 写入下界 | no-memory | 不使用记忆 | 是 |
| 固定臂 | 十个 uniform 臂 | 全库统一用某存法 | 是 |
| 固定臂 | best-fixed | 训练集上最优固定臂 | 是 |
| 写入上界 | full-extract(全量抽取) | 每轮全部构建增强(无准入、无选择),构建调用 = 1.0 的归一点 | 是 |
| 写侧路由 | MemRouter | 同检索/提示/骨干下仅准入策略不同,LoCoMo 52.0 vs 45.6 | 是 |
| 表示选择 | FluxMem(Style) | **把离线测得的动作效果压成一个 argmax 类别标签、用交叉熵训练择器**——这是与本文"学习逐动作筛后效应向量"最强的对照 | 是 |
| 消融 | R2W admission-only(− 形式轴) | 只做存在轴(none/raw),存则一律 raw | 是 |
| 消融 | R2W form-only(− 存在轴) | 全部入库、只做形式轴选择,没有 `none` | 是 |
| 消融 | R2W without-interference(− $\Psi$) | 净价值只用相关收益:$V^{-\Psi}(t)=\max_p[B_p-C_p]$,不入账干扰 | 是 |
| 消融 | R2W without-conservatism($\kappa{=}0$) | 去掉不确定度保守项($\kappa_E{=}\kappa_F{=}0$) | 是 |
| 模型族对照 | R2W-ridge | 岭回归双头(嵌入 + 特征 + 臂交叉) | 是 |
| 模型族对照 | R2W-GBM | pooled GBM value estimator(具体实现由 validation 确定) | 是 |
| 模型族对照 | R2W-kNN | 纯案例回归基线(§4.2.4) | 是 |
| 自适应 | R2W(主系统) | 存在轴 + 逐臂 pooled 择形 + 草稿证书预检 + 原文兜底 | 是 |
| 参照 | independent-action oracle | 每条记忆在"其余全原文"背景下取该条局部最优形式;局部最优非全局上界,用于诊断 | 是 |
| 外部系统 | Mem0 / Zep / A-Mem / MemoryOS | 官方实现,同一 harness 复现 | **是** |
| 外部:RL | Memory-R1 / AgeMem | 训练成本高且 harness 口径不同,仅引原文数字 | 参考 |
| 外部:写侧 | Dual-Layer | 与存在轴最近 | 参考 |
| 外部:写侧 | ConsistencyGate / A-MAC | 焦点在保真 / 五因子 | 参考 |

外部系统一律以官方实现在同一 matched harness 下复现进主表;RL 类系统(Memory-R1 / AgeMem)不进主表,仅引用原文数字并标注口径不可比。

**关键:必须与"把动作压成 argmax 标签"的简单基线正面对打。** FluxMem 式的做法是"所有候选离线跑一遍 → 取 argmax 作为监督标签 → 交叉熵训练"。如果 R2W 保留逐动作的筛后反事实效应向量 + 排序 + 不确定度,并没有明显胜过这个更简单的基线,那么"逐动作效应向量"这个贡献就不成立。这是本文 baseline 表里最重要的内部对照。

## 6.4 实施细节

超参一律指向附录 D 超参总表。决策参数 $(\lambda_w,\lambda_r,\lambda_s,\kappa_E,\kappa_F)$ 用普通 validation 选择;$\theta_{\text{fid}}=0.95$ 与 $\epsilon_0=0$ 为预置工程标准、走敏感性报告。构建契约与证书预检是硬规则,不合即弃草稿,不存在要调的门槛。

---

# 7 实验结果与分析

> 本节所有实数分三类:(a) 已跑实验并附日志/表格来源;(b) 引自他人论文并标注引用;(c) 方法层面的先验估计,一律标注"预期",且集中在第 7.1 节的"预期结果"小节,与实际结果对照呈现。凡未跑出的单元格一律标【待实测】。成本是会计、精度是推断,叙事顺序不许颠倒。

## 7.0 每个贡献对应什么实验:一张可证伪的对照表

正文把创新点拆成八个可证伪的组合点,下面的实验设计逐一给每个点一个可证伪的实验,并写明"什么结果会推翻它":

| 创新点 | 对应实验 | 什么结果会推翻它(证伪线) |
|---|---|---|
| ① 存在/形式两轴分解 | 两轴消融(all-raw / admission-only / form-only / 主系统,§7.4.2) | 若 admission-only ≈ all-raw → 存在轴没有独立价值;若 form-only ≈ all-raw → 形式轴没有独立价值;若 admission-only ≈ 主系统 → 收益几乎都在存在轴、形式轴增量价值有限,叙事重心应收缩到存在轴。**两轴要各自独立挣到钱,而不只是结构上分开。** |
| ② 查询分布统一单位(π_t 加权) | 消融:去掉 π_t,改回"相关查询求和 / 无关查询取平均" | 若去掉后 ΔF1 几乎不变 → "统一期望单位"是形式主义,不是必要修正。 |
| ③ 干扰 $\Psi_p$ 入账 | without-interference($V^{-\Psi}(t)=\max_p[B_p-C_p]$,只测相关收益)vs 主系统($\max_p[B_p-\Psi_p-C_p]$) | 若两者 ΔF1 几乎相同 → "干扰感知"不带来收益,创新点③站不住。 |
| ④ 逐动作筛后效应向量 vs argmax 标签 | R2W(筛后效应向量)vs FluxMem-argmax | 若 R2W 未明显胜过"候选跑一遍→取 argmax→交叉熵" → "逐动作效应向量 + 排序 + 不确定度"的贡献不成立。 |
| ⑤ pooled 逐臂模型与 estimator 选择 | GBM 主线 vs Ridge / NS / MLP 审计;另对照每臂独立模型(§7.4.1) | 若 GBM 未稳定优于更简单 estimator,则采用更简单者;若每臂独立模型 ≈ pooled,则 pooled 共享结构的必要性有限。 |
| ⑥ E 族索引状态特征 | 消融:去掉 E 族 | 若去掉后 raw背景/策略背景排序一致性(Kendall-τ)几乎不变 → E 族与多背景训练是多余复杂度。 |
| ⑦ 非参数案例基线 | 纯 kNN 案例回归 vs 主模型(§4.2.4) | 若打平 → 主模型复杂度没挣到钱,按判读规则收缩结论。 |
| ⑧ 不确定度保守项($\kappa_E,\kappa_F$) | without-conservatism($\kappa{=}0$)vs 主系统 | 若 $\kappa{=}0$ 下有害切换与误判 `none` 两类错误率不恶化、F1 不降 → 保守项价值有限,如实下调其贡献。 |

这张表让每个标准工具的边际价值都对应一个可证伪的结局,而不是只加不审。


## 7.1 研究假设:驱动下面这些实验的问题,而非结论

在动手跑主实验前,我们把 R2W 想验证的**研究假设**写清楚。这些是"我们要测什么、为什么这么测",**不是**已经得到的结果——所有数值都必须等实测,我们不下任何预测性的绝对 F1 数字。唯一提前说明的是**成本侧的记账关系**(它们在定义上是确定的,与实验无关)。

**假设 H1(存在轴价值)**:存在轴判断"这条记忆值不值得占容量",能带来独立于形式轴选择的收益(即"n 存必有、粗存漏"之外,存在本身也值得测)。对应实验:§7.2.1 的 graceful degradation ladder、§7.4.2 的两轴消融。

**假设 H2(表示轴价值)**:在"决定要存"的前提下,选择不同索引卡形式能进一步提升检索/答题。对应实验:§7.2.1 的 L2/L4 对比、§7.3.3 的卡价表。

**假设 H3(干扰是主要矛盾之一)**:把一条记忆的负外部性 $\Psi_p$ 计入净价值,能带来可观测的增益(尤其对抗题)。对应实验:§7.4.1 的"without-interference"。若该增益很小,则说明本文声称的"干扰感知"不是主要贡献,需如实下调。

**假设 H4(逐动作筛后效应向量 > argmax 标签)**:保留逐动作的筛后反事实效应向量 + 排序 + 不确定度,优于 FluxMem 式的"压成 argmax 再交叉熵"。对应实验:§7.4.1 的"vs FluxMem-argmax"。若打平,则"逐动作效应向量"这一贡献不成立。

**假设 H5(不确定度保守项的价值)**:向保留方向偏($+\kappa\hat\sigma$)主要换取更少的有害切换与更少的误判 `none`,未必大幅提升平均 F1。对应实验:§7.4.1 的 without-conservatism。若 $\kappa{=}0$ 下两类错误率几乎不变,我们在结论里如实下调保守项的贡献。

**成本侧的记账关系(定义上确定,非实验猜测)**:
- 期望构建调用数与全量抽取基线之比 **≈ $a\times k_{\text{draft}}/\lvert\mathcal P\rvert$**(先验记账口径:$0.24\times2.5/10\approx6\%$,含短名单草稿;全量抽取 = 每轮全部存法构建);
- 查询读取 token 相对 all-raw **≈ 0.7–0.85×**($sum$ 压缩 + none 减少无关条目);
- 索引规模相对全量抽取 **≈ 0.25–0.3×**(只有放行的轮次建索引卡)。

第 7.4.3 与 7.4.4 节会把这些连同每决策推理开销、以及离线重放成本一起报告——**成本是会计关系,精度是实验结果**,两者分开陈述,叙事顺序不颠倒。

> **成本的"另一面"**——整套离线反事实重放是一次性开销,换来的是在线不再用大模型 teacher 标签、不再逐轮自回归解码。系统级是否划算,取决于"离线重放预算"与"上线后每轮省下的 LLM 调用"的对冲;我们在 §7.4.4 报告这对冲关系(需多少部署轮次才能摊薄那一次离线成本)。

## 7.1.1 前提验证实验(先于主结果,检验该方法的两个关键前提)

在跑主结果之前,先做几个**判据式**实验:如果它们不成立,说明本文的定义或训练方式有问题,需要先修;如果成立,则下面的主结果才有意义。这也是本文"自证"的一部分。

**前提验证 1:存在轴必须用"存 vs 不存"而非"表示 vs 原文"。** 对若干采样记忆,分别计算 $\Delta^{E}_{\texttt{raw}}$(这一条在场 vs 完全不在场)与 $\max_p\Delta^{F}_p$(最优形式 vs 原文),画散点图。先验判断是:会出现大量"$\Delta^{E}_{\texttt{raw}}\gg 0$ 但 $\max_p\Delta^{F}_p\approx0$"的点。如果出现,就**证明**了"用表示效应当存在效应会漏掉"存原文本身就有价值"的情形"这一缺陷,也证明新定义必要。

**前提验证 2:干扰必须依形式而异。** 分别测 $\Psi_{\texttt{raw}},\Psi_{\texttt{kv}},\Psi_{\texttt{event}},\Psi_{\texttt{graph}},\Psi_{\texttt{hq}}$。若它们几乎一样,那么"干扰是负外部性"主要不是形式特异性的来源;若差异明显,这张图就成为"干扰入账"这一 novelty 的有力证据。

**前提验证 3:raw 背景与策略背景下的形式排序是否一致。** 对同一记忆 $t$,计算 $\mathrm{rank}(\Delta^{F}_p\mid b^{\text{raw}})$ 与 $\mathrm{rank}(\Delta^{F}_p\mid b^{\pi})$ 的 Kendall-$\tau$。若很高(如 >0.8),说明"用全原文背景标签 + E 族条件化"合理;若只有 0.3–0.5,说明单背景标签不够,必须把多背景训练升级为必要修正(§4.1.2 层 2b 的判读规则,不许事后调)。

**前提验证 4(零点决策的自洽性诊断)。** 每次实验同报三个诊断量,都不声称统计保证:①存在轴可靠性——$\hat y_b-\bar C(x;\lambda)$ 分箱与实测 $\mathbb 1[V_{\text{store}}>0]$ 的可靠性图与排序相关;②决策流水审计——各存法的选中/回退率、以及"切换 ∧ 变差"的联合事件频率(以 28.1% 的先导测量作参照);③`none` 的影子命中——判 `none` 的记忆其后被影子索引检索进 top-$k$ 的比例。若标量整体偏移而排序良好,用全局温度修正即可,不改决策结构。

## 7.2 主结果(待实测)

**表 4｜主表:LoCoMo 与 LongMemEval(-S)× 各系统的 F1 / ACC(子类拆行)+ 成本四列。** 第三个数据域 MSC 仅作附录稳健性(附录 G);外部系统均为官方实现在同一 matched harness 下复现,RL 类系统仅引原文数字参考、口径不可比。

**LoCoMo——端到端 QA(固定 reader,官方 token-F1)。**

| 系统 | 单跳 | 多跳 | 时间 | 开放 | 对抗 | 拒答率 | 整体 F1 | 构建 token | 构建调用 | 索引规模 | 查询读取 token |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| no-memory | 【待实测】 | | | | | | 【待实测】 | 0 | 0 | 0 | 0 |
| all-raw | 【待实测】 | | | | | | 【待实测】 | 【待实测】 | 0 | 【待实测】 | 【待实测】 |
| Mem0(官方实现) | 【待实测】 | | | | | | 【待实测】 | 【待实测】 | 【待实测】 | 【待实测】 | 【待实测】 |
| Zep(官方实现) | 【待实测】 | | | | | | 【待实测】 | 【待实测】 | 【待实测】 | 【待实测】 | 【待实测】 |
| A-Mem(官方实现) | 【待实测】 | | | | | | 【待实测】 | 【待实测】 | 【待实测】 | 【待实测】 | 【待实测】 |
| MemoryOS(官方实现) | 【待实测】 | | | | | | 【待实测】 | 【待实测】 | 【待实测】 | 【待实测】 | 【待实测】 |
| MemRouter | 【待实测】 | | | | | | 【待实测】 | | | | |
| FluxMem-style(argmax 标签选择器) | 【待实测】 | | | | | | 【待实测】 | | | | |
| uniform arms(10 臂,报最优臂与全区间) | 【待实测】 | | | | | | 【待实测】 | | | | |
| best-fixed | 【待实测】 | | | | | | 【待实测】 | | | | |
| full-extract(全量抽取) | 【待实测】 | | | | | | 【待实测】 | 【待实测】 | 1.0(归一) | 【待实测】 | 【待实测】 |
| R2W-ridge(线性双头) | 【待实测】 | | | | | | 【待实测】 | | | | |
| R2W-GBM(pooled value estimator) | 【待实测】 | | | | | | 【待实测】 | | | | |
| R2W-kNN(纯案例基线) | 【待实测】 | | | | | | 【待实测】 | | | | |
| **R2W 主系统** | 【待实测】 | | | | | | 【待实测】 | 【待实测】 | 预期 ≈6% | 【待实测】 | 【待实测】 |
| raw-background independent-action oracle | 【待实测】 | | | | | | 【待实测】 | | | | |

**LongMemEval(-S)——跨域(token-F1 / LLM-judge)。**

| 系统 | 信息抽取 | 多会话 | 时序 | 知识更新 | 弃权 | 整体 |
|---|---:|---:|---:|---:|---:|---:|
| all-raw | 【待实测】 | | | | | 【待实测】 |
| Mem0(官方实现) | 【待实测】 | | | | | 【待实测】 |
| Zep(官方实现) | 【待实测】 | | | | | 【待实测】 |
| A-Mem(官方实现) | 【待实测】 | | | | | 【待实测】 |
| FluxMem-style(argmax 标签选择器) | 【待实测】 | | | | | 【待实测】 |
| best-fixed | 【待实测】 | | | | | 【待实测】 |
| **R2W 主系统** | 【待实测】 | | | | | 【待实测】 |

每个单元格回填时须附:运行日志来源、五个子类的拆分、对话级 bootstrap 区间、所引用的阅读模型与判分器版本哈希。

### 7.2.1 简单策略天花板(graceful degradation ladder)

这是回答"增加的复杂度是否值得"的**最关键一套实验**。做法是:只给系统不同程度的复杂度,看它能打到多少。如果"只是用测出来的效用做贪心"就能拿大部分收益,那后面堆的"逐臂效应学习 + pooled 结构 + 草稿预检 + 不确定度"就是在为很少的 ΔF1 付出很大的复杂度;反之,如果每加一层都有明显增益,复杂度就是值回票价的。记 $C_p=\lambda_w C^{\text{write}}_p+\lambda_r C^{\text{read}}_p+\lambda_s C^{\text{index}}_p$ 为折算到收益单位的总成本。所有层都建立在**同一套离线测量量**($b^{\mathrm{rel}}_p$、$\Psi_p$、$C_p$,以及形式轴的 $\Delta^F_p$)之上,区别只在"净价值如何构成、叠加多少复杂度":

| 层 | 策略 | 复杂度 | 整体 F1 | Δ vs all-raw |
|---|---|---:|---:|---:|
| L0 | 规则启发式(新颖度/时近阈值准入) | 最低(无测量) | 【待实测】 | 【待实测】 |
| L1 | 只用相关收益贪心,忽略干扰:$V^{-\Psi}(t)=\max_p\,[B_p-C_p]>0$ 就存(无 Ψ、无保守项) | 低 | 【待实测】 | 【待实测】 |
| L2 | L1 + 干扰入账:$\max_p\,[B_p-\Psi_p-C_p]=\max_p\,[\Delta^E_p-C_p]$(Ψ 只扣一次,不重复计) | 中 | 【待实测】 | 【待实测】 |
| L3 | L2 + 不确定度保守项($\kappa_E,\kappa_F$)+ 可行性硬约束与证书回退 | 中高 | 【待实测】 | 【待实测】 |
| L4 | 主系统(逐臂效应标签 + pooled 择形器 + E 族 + 草稿预检 + 不确定度保守项) | 高 | 【待实测】 | 【待实测】 |

**判读规则(写死,不许事后挑)**:
- 若 $\Delta F1(L1)$ 已经 ≥ 80% 的 $\Delta F1(L4)$,那么本文的贡献应聚焦在"**测量本身 → 用测出的效用直接决策**",后面的复杂模型不是核心贡献,要在结论里如实说;
- 若要为"逐臂效应学习 / pooled 结构 / 草稿预检"辩护,必须看到 $L1 \rightarrow L4$ 每一层都有**可观测的正向增益**,且增益大于该层增加的每决策推理开销;
- 若 $L1$ 就很差(说明"光有测量、不会用"不够),那恰好证明复杂度是必要的——这时正是为全文最重的模块给出证据。

> 这张表刻意把复杂度问题摆上台面:不是"把 10 个工具都接上",而是"用测出的效用做决策,并在每一层复杂度上给出边际收益"。它比列几十个微型消融更能决定论文的贡献定位。

## 7.3 四张分析表

### 7.3.1 预算-精度前沿(操作点只在验证集确定,不许测试集挑点)

扫描预算乘子 $\lambda_w$(其余 $\lambda_r,\lambda_s$ 同比例联动)得到预算—精度前沿。各操作点只在验证数据上确定,测试集只评一次,不许用测试集挑点;每个操作点的 $(\lambda,\kappa)$ 组合登记在案。

| 操作点 | 相对构建调用比 | 整体 F1 | 备注 |
|---|---:|---:|---|
| no-memory | 0 | 【待实测】 | 不用记忆 |
| all-raw | 0 | 【待实测】 | F1 下界,构建零调用 |
| R2W 主系统 | ≈0.06(先验) | 【待实测】 | 标注所用 $(\lambda,\kappa)$ 配置 |
| R2W(无预检/无护栏) | ≈0.06(先验) | 【待实测】 | 检验草稿预检与归因护栏的价值 |
| best-fixed | 1.0 | 【待实测】 | 训练集上最优固定臂 |
| full-extract(全量抽取) | 1.0 | 【待实测】 | 构建调用归一点 |
| raw-bg local oracle | — | 【待实测】 | 局部最优,不是全局上界 |


### 7.3.2 存在轴与粗筛专项

| 指标 | 定义 | 先验估计 | 实测 |
|---|---|---:|---:|
| 存在轴放行率 $a$ | 被判 store 的轮次占比 | ≈ 0.24(与判断器完美对齐引用时 = 24.31%) | 【待实测】 |
| `none` 分流比例 $1-a$ | 被判 none 的轮次占比 | ≈ 0.76 | 【待实测】 |
| none 记忆影子命中轨迹 | η 影子索引上,这些记忆被检索进 top-k 的查询比例 | 低(证明判 none 的代价受控) | 【待实测】 |
| 漏存事件率 | 实测 $V_{\text{store}}>0$ 却被判 `none` 的比例(描述性指标,非保证) | 低,且随 $\kappa_E$ 增大而减小 | 【待实测】 |
| Ψ 估计收敛曲线 | 干扰成本估值 vs 真实 | 随无关查询数收敛 | 【待实测】 |
| Ψ±50% 敏感性 | 干扰成本 ±50% 下 $a$ 与 F1 的变化 | 待测 | 【待实测】 |
| 粗筛误剪最优臂概率 | 层 2 实测的最优动作被层 1 剪枝错杀的比例 | 低(≈0) | 【待实测】 |

### 7.3.3 形式轴:索引卡按内容类型的"卡价表"(表 5)

择形器不止给出每个决策的答案,还能回答**不同内容类型下,每种索引卡值多少钱**:对 pooled 模型的留出集预测按(存法 × 内容类型)分组,报告 $\mathbb E[\Delta\hat E_p\mid \text{存法,内容类型}]$。这张"卡价表"是本文与所有黑盒选择器最直观的区别:不用看任何例子,只要读这一张表,就知道哪种卡对哪类内容收益最大、成本几何。

**表 5｜索引卡"卡价表"($\mathbb E[\Delta\hat E_p\mid \text{存法,内容类型}]$,留出集上按组分组的预测效应均值;待实测填充,括号内为 95% 区间)。**

| 索引卡 | 实体/数值 | 时间/先后 | 关系/多跳 | 用户偏好 | 写出成本 |
|---|---:|---:|---:|---:|---:|
| `kv` | 正高 | … | … | … | 略多 |
| `event` | … | 正高 | … | … | 略多 |
| `graph` | … | … | 正高(只做索引卡) | … | 略多 |
| `hq` | … | … | … | … | 略多 |
| `sum`(压缩) | 负(信息被压掉) | 负 | 负 | … | **更省** |

> **读法**:`kv` 卡对时间类问题提升有限,但对实体/数值类问题贡献最大;`graph` 卡的价值集中在关系/多跳,且规定只做索引卡、答案回正文取。这样"要不要配索引卡、配哪种"就变成了可读、可比、可审计的数字,而不是黑盒选出的一个动作。这比"某个模型选出了结果"可解释得多。



### 7.3.4 结局口径与归因护栏:$(\Delta g,\Delta u)$ 同报、Pareto 轨迹、护栏消融

价值函数只含端到端结局 $u$(§3.3)。本节回答审稿人最可能追问的三个问题:

1. **收益可归因吗?** 同报逐记忆的 $(\Delta g,\Delta u)$ 联合分布与边际分布:若 $\Delta u>0$ 而 $\Delta g\approx0$ 的记忆大量存在,要么存在归因断裂(答案变好但证据并没被检到),要么阅读模型从公共块而非检索产物获益——必须拆开解释,不许含糊。
2. **Pareto 轨迹。** 用 §7.3.1 同批操作点画 $(\Delta g,\Delta u)$ 的 Pareto 前沿,报告主操作点在前沿上的位置;主操作点应当在前沿上或紧邻前沿。
3. **护栏消融。** 主系统(护栏 $\epsilon_0=0$:否决 $\Delta u>0\ \wedge\ \Delta g<0$ 的动作)vs 无护栏变体。若无护栏下有害切换率或对抗题成绩恶化 → 护栏保留;若全无差别 → 护栏简化为报告口径,如实下调其必要性。

另作**历史口径对照**(不进正文结论):按旧版加权综合分口径在权重网格 $\{0,0.25,0.5,0.75,1\}$ 上复测主系统,仅用于证明主结论不依赖已移除的效用构造;该表标注"历史口径,非主指标"。

| 指标 | 定义 | 判读 |
|---|---|---|
| $(\Delta g,\Delta u)$ 联合分布 | 逐记忆的检索增量 × 结局增量 | $\Delta u>0$ 且 $\Delta g<0$ 占比过高 → 归因存疑,拆开解释 |
| Pareto 前沿 | 操作点扫描下的 $(\Delta g,\Delta u)$ 轨迹 | 主操作点应在前沿上或紧邻 |
| 护栏消融 | $\epsilon_0{=}0$ 护栏 vs 无护栏 | 有害切换 / 对抗题恶化 → 保留护栏;无差别 → 降级为报告口径 |
| 历史口径对照 | 旧加权综合分网格复测 | 主结论方向不变 → 效用构造不构成依赖,移除成立 |

**判读规则(写死,不许事后挑)**:主结论(相对 all-raw 的 ΔF1 符号与量级、without-interference 差距、两轴消融方向)必须在"主口径 $u$"与"历史加权口径全网格"下方向一致;若只在某一口径下成立,结论如实下调为"在该结局口径下成立"。

## 7.4 组件是否值得:复杂度 ⟷ 收益账本

前面几节做了"去掉某组件"的消融。但只报"去掉后掉了多少"还不够——因为每个组件都**增加了复杂度**(更贵的特征、更重的模型、一次离线重放、甚至一次重训练),所以必须同时报"它增加了多少复杂度"和"它换回了多少精度",否则无法回答"这复杂度值不值"。这就是把消融从"去掉 X"重构为"X 这个复杂度买到了什么"。

### 7.4.1 复杂度账本(每个组件一行:复杂度 / 设计目标 / 获得的 ΔF1 / 代价 / 判定)

| 组件(增加的复杂度) | 设计目标(它应该买到什么) | ΔF1(去掉它) | 它增加的复杂度 | 判定 |
|---|---|---|---:|---|
| 存在轴 $\Delta^E$(相对"表示效应") | 别把"存原文本身就有价值"误判成 none | 【待实测】 | +一组缺席测量(≈$1/\lvert P\rvert$ 开销) | 若 ΔF1 明显为正 → 保留;否则说明两轴分解是形式主义 |
| 查询分布统一单位(π_t 加权) | 消除"相关求和/无关取平均"的量纲错位 | 【待实测】 | 无额外测量,只改收益口径 | 若 ΔF1 为正 → 必要修正;否则是锦上添花(如实说) |
| $\Psi_p$ 干扰入账($\max_p[B_p-\Psi_p-C_p]$ vs $\max_p[B_p-C_p]$) | 测出"对别的查询"的负外部性 | 【待实测】 | +无关查询抽样(层 2 的 10–20%) | 若 ΔF1 为正 → 创新点③成立;否则说明干扰不是主要矛盾 |
| E 族索引状态特征 | 修"raw 背景 vs 策略背景"排序反转 | 【待实测】 | +10 维写时特征(几乎零成本) | 若 Kendall-τ 与 ΔF1 一起改善 → 保留;否则是多于 |
| pooled 逐臂 GBM(审计:Ridge / NS / MLP) | 逐臂 value 幅度保留 + 非线性交互 | 【待实测】 | 一次模型训练与 estimator 审计 | GBM 不胜更简单 estimator → 用更简单者;每臂独立模型 ≈ pooled → 舍 pooled 结构 |
| 逐动作筛后效应向量 + 排序 + 不确定度(vs argmax) | 保留相对偏好,而非压平标签 | 【待实测】 | 训练目标更重 | 若明显胜过 FluxMem-argmax → 核心贡献;否则不是 |
| 不确定度保守项($\kappa_E+\kappa_F$) | 不确定时向保留方向偏 | 【待实测】 | 集成头 + 两个标量,在线零额外 | 若显著降有害切换与误判 `none` → 保留;否则调小 $\kappa$ 并如实报告 |
| kNN 局部特征(距离、近邻效应均值) | 把局部性交给模型自己用(§4.2.4) | 【待实测】 | +2 维特征(几乎零成本) | 去掉后 ΔF1 不降 → 删特征 |
| 短名单草稿构建 + 证书预检 | 产物性质从"预测"换成"测量",消灭上线后回退 | 【待实测】 | +(短名单长−1)×220M 小构建(仅放行轮)+ NLI 预检 | 回退率与净 F1 无改善 → 退回"构建一个、事后验收"旧结构 |

**判定是给审稿人的**:上表每一行我都给了"什么结果会让人工降低这层复杂度"的明确条件。这比报"去掉它掉了 X"更能回答"这个复杂度值不值"。

### 7.4.2 两轴消融:存在轴与形式轴各自挣了多少

从主系统出发逐级**去掉**一条轴,就能把两条轴的独立贡献拆开——这四行比几十个微型消融更能决定论文的叙事重心:

| 配置 | 消融了什么 | 剩余机制 | 整体 F1 | Δ vs all-raw |
|---|---|---|---:|---:|
| 主系统(full) | — | 存在轴 + 形式轴 + 干扰入账 + 保守项与回退 | 【待实测】 | 【待实测】 |
| − 形式轴(admission-only) | 形式轴 | 只做存在轴(none / raw),存则一律 raw | 【待实测】 | 【待实测】 |
| − 存在轴(form-only) | 存在轴 | 全部入库,只做形式轴选择,没有 `none` | 【待实测】 | 【待实测】 |
| − 两轴(all-raw) | 两轴 | 全量存原文,无准入无选择(基线) | 【待实测】 | 0 |

**判读规则(与 §7.0 ① 同口径,写死不许事后挑)**:存在轴的独立贡献 = F1(主系统) − F1(form-only);形式轴的独立贡献 = F1(主系统) − F1(admission-only)。若 admission-only ≈ all-raw,说明存在轴没有独立价值,创新点①不成立;若 form-only ≈ all-raw,说明形式轴没有独立价值;若 admission-only ≈ 主系统,说明收益几乎都在存在轴、形式轴的增量价值有限,论文叙事重心应相应收缩到存在轴。两条轴必须各自独立挣到钱,而不只是结构上分开。

### 7.4.3 每决策推理开销(复杂度是否落在关键路径上,且付得起)

数据管理视角的论文一定会追问:你加了这么多特征和模型,每写一条记忆到底多慢?这里和两个写侧强基线对齐——MemRouter 用过的冻结骨干 + 12M 头(写侧 p50 仅 58 ms),以及 LLM 管理器(写侧 p50 约 970 ms)。R2W 增加的复杂度是否付得起,必须在这个口径下汇报:

| 决策环节 | 每决策推理开销 | 备注 |
|---|---:|---|
| 存在轴第一关(M 族 + 廉价 I/E 索引状态) | 0 次生成调用(嵌入复用索引构建 + 一次 ANN 查询,与 §4.2.1 一致) | 大部分轮次在此终止 |
| 形式轴第二关 | 220M 出题 + 2–3 个短名单草稿(本地小构建)+ NLI 证书预检 + 逐臂前向 | 仅约 24% 的轮次进入 |
| E 族索引状态特征 | 若干索引统计量(接近零) | 写时已可观测 |
| 决策规则(两行标量比较) | 在线只是标量比较,不在关键路径 | — |
| 执行 | 草稿即产物,无二次构建 | — |

**结论口径**:期望构建调用约为全量抽取基线的 **6%**(先验记账:放行率 × 短名单长 / 存法总数,待实测);在线决策路径不调用大模型、不用生成器逐轮解码。大模型只出现在离线测量的 reader 答题与 judge 判分两处(§4.1.4),属一次性离线开销,其摊销见 §7.4.4。把"加了复杂度"和"关键路径上没多花多少"放在同一张表里,回答的是"复杂度是否划算"这个问题。

### 7.4.4 离线测量成本是一次性的,且要算清楚它值不值

整套反事实重放是一次性离线成本,其中调用大模型的只有两处:阅读模型真实答题(层 1 全量 + 层 2/3 局部)与判分模型评分(§4.1.4);其余是索引重建、单条替换、检索与统计,均为非大模型开销。它只付一次,换取在线两项成本归零:不再用大模型产出 teacher 标签、写入决策路径零大模型调用。这里把"离线重放预算 vs 上线后每轮省下的 LLM 调用"对冲成一张账,直接报告需要多少部署轮次才能摊薄那次离线成本的盈亏平衡点——离线重放的每一分钱都要在这本账上见到回头钱。

`none` 改成 `delete` 的危害对照是本文立场的关键证据:它直接量化"归档而不删除"的价值——若把 `none` 改成删掉原文,漏存的记忆就无法恢复,预期整体 F1 显著下降。这是把"none ≠ 删除"从口号变成可测结论的实验。

# 8 讨论

## 8.1 R2W 适用在哪里,边界在哪里

R2W 的全部结论建立在三个条件上:**写一次**(写入动作在写入后定型)、**固定检索器**(测量配置 = 部署配置)、**局部动作选择**(每条记忆独立决策)。在这个设定内,索引可重建带来 per-decision full feedback,写入价值可以测而不必猜。一旦超出这个设定——引入更新、合并、删除、巩固这类会跨时间步改变索引的动作,或者容量存在长程耦合约束——"每个动作的单个结局"就不再能逐个重放,强化学习或动态规划仍是更合理的工具。我们把适用范围写在方法内部,而不是埋在脚注里。

第二个边界是需求的可预测性。如果未来查询与记忆内容基本无关(外生随机需求),任何写侧方法都无法事前预测——这是信息论上界,不是某个系统的缺点。R2W 的回答是把失败模式做成可恢复的:`none` ≠ 删除、不确定就往存的方向偏($\hat y_b-\bar C(x;\lambda)+\kappa_E\hat\sigma$)、读侧置信不足时向冷归档扩召回(§4.5)。缓解不等于消除,"未知的未知用途"留在保证之外,这一点我们不回避。

第三个边界是数据与分布。净价值承诺条件于部署查询分布 $\mathcal D_Q$ 与 $\Psi$ 的估计质量;换数据域、换检索器、换判分器,都需要重新测量与重训。模型与配置同版本冻结、原子切换——线上跑的和验证过的不是同一个系统的混用,是被明确禁止的。对"没有 gold evidence 的真实 agent 日志",附录 H 给出三种自动 evidence 来源的退化实验,它是 R2W 适用面的试金石:若不坍塌,核心机制与手工标注解耦;若坍塌,"高保真标签"应如实列为前提。

## 8.2 这套方法真正付出的成本

**主成本是一次性离线重放预算。** 其中调用大模型的只有两处:阅读模型真实答题与判分模型评分(§4.1.4)——它们是被测系统自身的消耗,R2W 不叠加任何大模型 teacher。系统级是否划算,由摊销轮次回答,§7.4.4 给出盈亏平衡点,而不是含糊带过。

**汇率声明与护栏是本版仅存的设计自由度。** 收益/成本折算靠显式声明的 $\lambda$(验证集选点 + 敏感性报告),归因护栏 $\epsilon_0$ 预置 0(消融见 §7.3.4);旧版"检索与答题各半加权"的效用构造已被移除(§3.3 的三条理由),仅留作 §7.3.4 的历史口径对照。这是本版相对上一稿最实质的简化:价值函数里不再有任何不可识别的权重。

**"在原则上可测"不等于"每个动作都测"。** 粗筛剪枝换来成本,代价是"筛后"口径:最优臂被误剪的概率作为公开诊断量,粗筛过激会把该数字直接推高(§7.3.2)。这是我们主动留下的可检验性,而不是隐藏假设。

## 8.3 风险如何处理,失败时会发生什么

围绕风险,本文的机制是决策结构本身,而不是统计证书:

- **没有统计保证,有确定性回退与可恢复性。** 决策只用两行规则、保守项与两类硬检查(契约 + 证书预检)。我们不提供分布无关的错误率上界——评估过把风险校准家族搬到写侧的方案并放弃(§4.3),因为被校规则与执行规则难以同一:与其让一致性依赖持续重校准,不如让它来自定义。
- **失败方向刻意做成保守。** 预测误差大时 $+\kappa\hat\sigma$ 主导:多存原文、边缘样本倾向存——错也错在"浪费一点容量",而不是"激进地改坏表示"。"只防错不防漏"仍是刻意的非对称:`raw` 是安全底,"该换没换"不在惩戒范围内。
- **干扰只被量化、不被根除。** 单条替换在干净背景测量,上线是混合背景;E 族特征、层 2b/层 3 审计、随机混存审计是三道防线,残余偏差如实报告,不声称为零。
- **测量链路有依赖项。** 小生成器质量决定 Q 族特征与 `hq` 卡的上限(不合格问题会被过滤);NLI 逐条比对抓不住"真话拼成假话"的组合骗术(靠冷归档可查与人审抽查兜底);`none` 带来的曝光偏差由 5% 影子索引缓解而非消除。每一项都有兜底,但兜底不是无事发生的承诺。
- **长期运行靠版本纪律。** 微调后模型与配置同版本原子切换,不允许混用;漂移只做看板监测与人工重新评估;层 3 审计结果不稳定时只作实现启发式表述,不当结论引用。
## 8.4 从"启发式写"到"测量写":一套可以带走的方法学

本文最想留下的不是某个具体系统,而是一条可迁移的纪律:**每一层复杂度都必须有与之对应的、可证伪的收益。** 复杂度—收益账本(§7.4)的逻辑与 R2W 无关——如果只用测出的效用做贪心就拿走 80% 的收益,那论文的核心就应收缩到"测量"本身;复杂 learner 存在的意义必须逐层挣出来。我们希望这成为记忆系统、乃至更广 agent 组件论文的通行写法。另一条纪律同样写死:**GBM 只是默认 estimator,必须接受简单基线审计**——Ridge / NS / 小型网络的结果公开;若更简单的方法稳定打平或胜过 GBM,就采用更简单者。

写侧的可审计性是副产品,也是接口:每条记忆"为什么存、为什么不存"都附带预测收益、不确定度与参考案例,决策边界有明确语义(两行规则 + 保守项 + 构建期硬检查)。这使 R2W 可能成为医疗、合规等高风险场景里一个合格的记忆子组件——但落地需要领域特定的验证与监管配套,本文不做超出证据的断言。

反面同样要说清楚:**记忆准入也是一种权力。** 一个系统性地把某类内容判为 `none` 的策略,哪怕每个单条决策都有据可查,长期也会形成选择性的遗忘偏差。冷归档与影子索引保证的是"技术上可恢复",而"该不该忘、谁来复审、多久复核一次"是治理问题。为"忘掉什么"立规,我们把它列为明确的开放问题,而不是声称已经解决。

---

# 9 结论

长期记忆的写入决策——"存不存、存成什么"——长期缺乏依据:要么靠规则或语言模型即兴判断、无度量无回退,要么用强化学习试错、贵且脆。我们观察到,记忆系统的检索索引可以从对话记录完全重建,于是每个候选动作的结局在原则上都可通过索引重建反事实测量;实践中 R2W 先以低成本粗筛缩小候选集,再对存活动作执行单条重放——问题从"试错"变成"直接测量"。这里限定在"写一次、固定检索器、只做局部动作选择"的设定(局部决策的 per-decision full feedback);一旦引入更新/合并/删除/巩固等跨时间步动作,问题不再退化为一串独立的监督决策,本方法不再适用。据此,我们提出 R2W:用两个不同的决策轴(存在轴 × 十种形式轴)、分量化的逐臂反事实测量标签(三层测量 + 净值决策时合成)、以及只有两行的决策规则(没把握就往保留方向偏,草稿先持证再上场),把写入决策变成一门有零点、有对照、有可回退结构的测量;决策路径不调用大模型(大模型只出现在离线测量中,复用被测系统自身的阅读与判分模型,见 §4.1.4),构建调用先验约为全量抽取基线的 6% 量级(含草稿,实测核准)。

我们想把"存不存"从工程经验变成一门**有零点、有对照、可回退**的测量科学——像数据库的物理设计一样,由对工作负载的测量驱动,而不是由启发式驱动。

---

# 参考文献

> 编号为正文引用顺序的稳定标签,正文各处引用与本表逐条对应、无重复无遗漏。标 ⚠ 者引自律所限的检索信息,投稿前建议按原文核对;标 ✅ 者已核对标题/编号。

1. Zhang et al. **Adaptive Memory Admission Control for LLM Agents (A-MAC).** arXiv:2603.04549 (2026-03). ⚠. 五因子准入;效用为单次语言模型启发打分;原文自认"a poorly specified and weakly controlled component";LoCoMo F1 0.583。 ✅
2. Yan et al. **Memory-R1: Enhancing LLM Agents to Manage and Utilize Memories via RL.** arXiv:2508.19828 (2025-08). ✅. {ADD, UPDATE, DELETE, NOOP} + PPO/GRPO;152 个问答对。
3. Yu et al. **AgeMem**(三阶段 GRPO). arXiv:2601.01885 (2026-01). ⚠.
4. Hu, Lin, Zhang, Ma, Wang. **MemRouter: Memory-as-Embedding Routing for Long-Term Conversational Agents.** arXiv:2605.00356 (2026-05). ✅. 写侧二元准入解耦;冻结骨干 + 约 12M 参数;LoCoMo matched harness F1 52.0 vs 45.6;附录:GRPO-only 冷启动全 NOOP 塌缩、UPDATE 模式坍缩。
5. Yang, Chen, Zhuang, Fan et al. **RoMeRL: Balancing Feedback Coverage and the Memory-Reward Trap in Self-Evolving Agent Memory via Reduced-Order Utility States.** arXiv:2608.02508 (2026-08). ✅. 修强化学习记忆的 memory-reward trap。
6. Du. **Memory for Autonomous LLM Agents: Mechanisms, Evaluation, and Emerging Frontiers.** arXiv:2603.07670 (2026-03). ✅. POMDP 形式化 + write–manage–read 循环;把 write-path filtering 列为工程开放问题。
7. Amiraz, Cuconasu, Filice, Karnin. **The Distracting Effect: Understanding Irrelevant Passages in RAG.** arXiv:2505.06914 (2025). ✅.
8. Cuconasu et al. **The Power of Noise: Redefining Retrieval for RAG Systems.** SIGIR 2024. arXiv:2401.14887. ⚠.
9. **Conformal Arbitrage: Risk-Controlled Balancing of Competing Objectives in Language Models.** arXiv:2506.00911 (2025). ✅.
10. **Conformal LLM Routing with Distribution-Free Safety.** ACL 2026 (SRW). ✅.
11. Packer et al. **MemGPT: Towards LLMs as Operating Systems.** arXiv:2310.08560. ✅.
12. Kang, Ji, Zhao, Bai. **Memory OS of AI Agent (MemoryOS).** EMNLP 2025. arXiv:2506.06326. ✅.
13. Li et al. **MemOS: A Memory OS for AI System.** arXiv:2507.03724 (2025). ✅.
14. Zhong et al. **MemoryBank: Enhancing Large Language Models with Long-Term Memory.** AAAI 2024. ✅(Ebbinghaus 遗忘曲线衰减).
15. Chhikara et al. **Mem0: Building Production-Ready AI Agents with Scalable Long-Term Memory.** arXiv:2504.19413 (2025). ✅.
16. Xu et al. **A-Mem: Agentic Memory for LLM Agents.** arXiv:2502.12110 (2025). ⚠.
17. Rasmussen et al. **Zep: A Temporal Knowledge Graph Architecture for Agent Memory.** arXiv:2501.13956 (2025). ⚠.
18. Anokhin et al. **AriGraph.** IJCAI 2025. ⚠.
19. Wang & Chen et al. **MIRIX: Multi-Agent Memory System for LLM-Based Agents.** arXiv:2507.07957 (2025). ✅.
20. Wang et al. **O-Mem: Omni Memory System for Personalized, Long Horizon, Self-Evolving Agents.** arXiv:2511.13593 (2025). ✅.
21. Pan et al. **On Memory Construction and Retrieval for Personalized Conversational Agents (SeCom).** arXiv:2502.05589 (2025). ✅.
22. **From Single to Multi-Granularity (MemGAS).** arXiv:2505.19549; ICLR 2026. ✅.
23. Gutiérrez et al. **From RAG to Memory (HippoRAG 2).** arXiv:2502.14802 (2025). ⚠.
24. **Infini Memory: Maintainable Topic Documents for Long-Term LLM Agent Memory.** arXiv:2606.10677 (2026). ✅.
25. **MemoChat**(让模型把对话对照写成结构化 memo 供检索)、**LightMem**(低开销记忆增广管线)、**SimpleMem**(少组件、易部署的终身记忆管线)、**MemEvolve**(跨会话自演化的记忆知识库)、**Cognee**(开源图记忆后端与语义层)——五个近期系统的 arXiv 出处随评测文献 26 统一登记 ⚠。
26. Zhou, Zhou, Han, Xu, Li, Li, Xiong, Wu. **Are We Ready For An Agent-Native Memory System?** arXiv:2606.24775 (2026). ✅. 数据管理视角;四模块框架(表示/抽取/路由/维护);12 个系统 × 5 负载;识别"写入时覆盖保持的抽取"最稳。
27. Lu et al. **Choosing How to Remember: Adaptive Memory Structures for LLM Agents (FluxMem).** arXiv:2602.14038 (2026-02). ✅. 自适应结构选择;单点取最大 vs R2W 逐动作效应向量 + 保守决策;PersonaMem +9.18%、LoCoMo +6.14%。
28. **BudgetMem.** arXiv:2602.06025 (2026-02; ICML'26). ⚠. 读侧运行时预算路由(强化学习);R2W 写入时决策、压缩轴覆盖其档位轴、零强化学习。
29. Luo, Xu, Yang. **MemSIF: From Structured Interactions to Dual-Track Fact Memory for LLM Agents.** arXiv:2608.01742 (2026-08). ✅. 定义 TSM 与 DUM;双轨事实记忆 CoreFact/ActiveFact 事后追认;LoCoMo 75.62 / LongMemEval-S 最佳。
30. Li, Nie, Lan, Lyu et al. **Dual-Layer Agentic Memory with Fast Write Routing and Slow Consolidation.** arXiv:2608.22215 (2026-08). ✅. non-write / write-new / write-update 三分写入路由 + 小到大级联 + 慢速巩固;判据为冗余/启发式,无测量、无保证。
31. **ConsistencyGate: Preventing Memory Contamination in LLM Agents via Self-Consistency Admission Control.** arXiv:2607.22962 (2026-07). ✅. 写入侧保真闸门,只查事实支持、明确不查效用;阈值(建议 τ=0.7)为手调、无校准。
32. Wang, Dong, Liang, Zhang, Luo, Liu, Xue, Tang. **MemGuard: Persisting Verifier Signals for LLM-Agent Memory Governance.** arXiv:2608.21867 (2026-08). ✅.
33. **Governing Evolving Memory in LLM Agents (SSGM).** arXiv:2603.11768 (2026). ✅. 巩固前强制一致性校验、时间衰减与访问控制。
34. Fan, Liu, Yang, Ouyang, Han. **Can Agent Memory Systems Track Evolving State? (StateMem / StateMemBench).** arXiv:2608.19652 (2026-08). ✅. 状态追踪基准,234 个多会话场景;closed-pool 评分区分当前/被取代/其他失败。
35. **Learning When to Remember: Risk-Sensitive Contextual Bandits for Abstention-Aware Memory Retrieval (RSCB-MC).** arXiv:2604.27283 (2026). ✅. 读侧记忆控制器,含 abstention 动作。
36. Rubin 1974; Imbens & Rubin. **Causal Inference for Statistics, Social, and Biomedical Sciences.** 2015. ✅.
37. Hu, Wang, McAuley. **Evaluating Memory in LLM Agents via Incremental Multi-Turn Interactions (MemoryAgentBench).** ICLR 2026. arXiv:2507.05257. ✅. 四项认知能力。
38. He et al. **MemoryArena: Benchmarking Agent Memory in Interdependent Multi-Session Agentic Tasks.** ICML 2026. ✅.
39. Tan et al. **MemBench.** ACL 2025 Findings. ✅(区分事实型与反思型记忆;参与/观察模态)。
40. Wu et al. **LongMemEval-V2: Evaluating Long-Term Agent Memory Toward Experienced Colleagues.** arXiv:2605.12493 (2026). ✅. 451 题,web agent 轨迹,最深 115M token。
41. **AMA-Bench: Evaluating Long-Horizon Memory for Agentic Applications.** arXiv:2602.22769 (2026). ✅.
42. **EvoMemBench: Benchmarking Agent Memory from a Self-Evolving Perspective.** arXiv:2605.18421 (2026). ✅.
43. **Useful Memories Become Faulty When Continuously Updated by LLMs.** arXiv:2605.12978 (2026). ✅. 保留原始 episode 的 agent 准确率是强制巩固者的两倍。
44. Ma & Tresp. **Causal Inference under Networked Interference.** AISTATS 2021, PMLR 130. ✅.
45. Aronow & Samii. **Estimating Average Causal Effects Under General Interference.** AOAS 2017. arXiv:1305.6156. ✅.
46. Angelopoulos, Bates, Fisch, Lei, Schuster. **Conformal Risk Control.** ICLR 2024. arXiv:2208.02814. ✅.
47. Barber, Candès, Ramdas, Tibshirani. **Conformal Prediction Beyond Exchangeability.** Ann. Stat. 2023. arXiv:2202.13415(分裂 conformal / 非交换);Dunn, Wasserman & Ramdas, JASA 2023, arXiv:1809.07441(分层). ⚠.
48. Xu, Guo, Wei. **Selective Conformal Risk Control (SCRC).** arXiv:2512.12844 (2025). ✅.
49. Gibbs & Candès. **Adaptive Conformal Inference Under Distribution Shift (ACI).** NeurIPS 2021. arXiv:2102.06230. ✅.
50. Kiryo, Niu, du Plessis, Sugiyama. **Positive-Unlabeled Learning with Non-Negative Risk Estimator (nnPU).** NeurIPS 2017. arXiv:1703.00565. ✅.
51. Elkan & Noto. **Learning Classifiers from Only Positive and Unlabeled Data.** KDD 2008; du Plessis et al. 2015/2017. ✅.
52. Dudík, Langford, Li. **Doubly Robust Policy Evaluation and Learning.** ICML 2011. ✅.
53. Swaminathan & Joachims. **The Self-Normalized Estimator for Counterfactual Learning.** NeurIPS 2015. ✅.
54. Vapnik & Vashist. **Learning Using Privileged Information.** 2009. ✅.
55. Burges. **From RankNet to LambdaRank to LambdaMART.** 2010. ✅.
56. Kendall & Gal. **What Uncertainties Do We Need in Bayesian Deep Learning?** NeurIPS 2017. ✅.
57. Qin & Liu. **Introducing LETOR 4.0 Datasets.** 2013. ✅.
58. Robertson & Zaragoza. **The Probabilistic Relevance Framework: BM25 and Beyond.** FnT IR, 2009. ✅.
59. Cormack, Clarke, Buettcher. **Reciprocal Rank Fusion.** SIGIR 2009. ✅.
60. Gao et al. **Precise Zero-Shot Dense Retrieval without Relevance Labels (HyDE).** 2022. ✅.
61. Min et al. **FActScore.** EMNLP 2023. arXiv:2305.14251. ⚠.
62. Zha et al. **AlignScore.** ACL 2023. arXiv:2305.16710. ⚠.
63. **Long-Form Information Alignment Evaluation Beyond Atomic Facts (MontageLie).** arXiv:2505.15792 (2025). ⚠.
64. Maharana, Lee, Tulyakov, Bansal, Barbieri, Fang. **Evaluating Very Long-Term Conversational Memory of LLM Agents (LoCoMo).** ACL 2024. arXiv:2402.17753. ✅.
65. Wu, Wang, Yu, Zhang, Chang, Yu. **LongMemEval: Benchmarking Chat Assistants on Long-Term Interactive Memory.** ICLR 2025. arXiv:2410.10813. ✅.
66. Colaco & Lahjouji. **What to Keep, What to Forget: A Rate–Distortion View of Memory Compaction in LLMs and Agents.** arXiv:2607.08032 (2026-07). ✅. 层无关下界;query-agnostic 压缩付出可量化惩罚;原则 P1"不可逆丢弃劣于可逆归档"。
67. Nogueira & Lin. **Document Expansion by Query Prediction (doc2query / docTTTTTquery).** arXiv:1904.08375, 1906.04416. ✅.
68. Gospodinov, MacAvaney, Ounis. **Doc2Query--: When Less is More.** SIGIR 2023. arXiv:2301.03266. ⚠(生成问题需过滤).
69. **Doc2Query++**(含 Weller et al. 2023"拼接伤稠密检索"与 dual-index 融合). arXiv:2510.09557 (2025). ⚠(对稠密检索需独立编码).
70. Khandelwal, Levy, Jurafsky, Zettlemoyer, Lewis. **Generalization through Memorization: Nearest Neighbor Language Models (kNN-LM).** ICLR 2020. arXiv:1911.00172. ⚠.
71. Borgeaud et al. **Improving Language Models by Retrieving from Trillions of Tokens (RETRO).** ICML 2022. arXiv:2112.04426. ⚠.
72. Wu, Rabinovich, Raganato et al. **Memorizing Transformers.** ICLR 2022. arXiv:2203.08913. ⚠. 层内缓存 key-value + top-k 近似检索。
73. Qian, Zhu, Lyu et al. **MemoRAG: Moving Towards Next-Gen RAG via Memory-Inspired Knowledge Discovery.** arXiv:2409.05591 (2024). ⚠. 全局记忆模型生成检索线索再检索。
74. Lee, Zhu, Jang, Jeong. **ReadAgent: A Human-Inspired Reading Agent with Gist Memory over Very Long Contexts.** arXiv:2402.09727 (2024). ⚠. 分页阅读 + gist 记忆。
75. Fountas et al. **Human-inspired Episodic Memory for Infinite Context (EM-LLM).** arXiv:2406.11460 (2024). ⚠. 贝叶斯惊奇切分情节事件。
76. Künzel, Sekhon, Bickel, Yu. **Metalearners for Estimating Heterogeneous Treatment Effects using Machine Learning.** PNAS 2019. arXiv:1706.03461. ⚠. S/T/X-learner 框架;本文取 pooled S-learner 范式。
77. Zheng et al. **Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena.** NeurIPS 2023 Datasets and Benchmarks. arXiv:2306.05685. ⚠. LLM 判分的位置/冗长偏差与成对判分协议。
78. Ke et al. **LightGBM: A Highly Efficient Gradient Boosting Decision Tree.** NeurIPS 2017. arXiv:1706.08359. ⚠. 效应学习候选模型族之一。

---

# 附录

## 附录 A 假设清单(含违反后果)

| # | 假设 | 白话 | 违反后果 |
|---|---|---|---|
| A1 | 检索配置训练 = 部署 | 测量时的检索器、阅读模型、判分器与上线一致 | 标签作废,重测 |
| A2 | 训练与部署分布相近 | 普通监督学习前提:标签与特征不漂移,或漂移可被周期重训吸收 | 性能退化,周期性重训 + 原子切换 |
| A3 | margin 条件 | 最优和次优存法差距别太集中 | 只是收敛变慢,方法仍成立 |
| A4 | 三种测量分清并用对地方 | 定义性问题(表 3) | 已由本文设计消除 |
| A5 | evidence 可用(训练期) | 需要"问题需要哪几轮"的标注 | 无标注场景用生成问题自举 |
| A6 | 生成器确定性 | 同输入同输出 | hq 存法的标签失效 |
| A7 | 参考索引规则固定 | 彩排用的索引每个时刻长得一样 | 特征漂移 |
| A8 | 预测误差分布稳定 | 余量还有效 | 可行性检查失准,重算余量 |
| A9 | 答题分的背景近似 | 统一背景测的答题分 ≈ 混合背景 | 层 3 复测兜底 |
| A10 | 查询分布 $\mathcal D_Q$ 的样本近似 | 用 benchmark 查询 + 抽样无关查询来估计 $\mathcal D_Q$ | 偏差由敏感性($\pi_t,\Psi\pm50\%$ 网格)与跨域(两数据域)检验 |
| A11 | 分词器固定 | 计数器不换 | 换了重标定 λ 和余量 |
| A12 | $\Psi$ 与 $\pi_t$ 抽样无大偏 | 无关查询抽样与命中率估计代表真实查询分布 | 增值判定偏;敏感性:$\Psi,\pi_t\pm50\%$ 网格 |
| A13 | 收益包络 + 确定性成本 ≈ 净值包络 | 第一关廉价 gate 的一阶近似(§4.2.2) | 一致率低则第一关改逐臂预测(更贵);一致率在验证集公开 |

## 附录 B 符号表

| 符号 | 含义 | 训练可见 | 部署可见 |
|---|---|---|---|
| $M(t),P_p(t),K_p(t)$ | 公共块 / 正文 / 索引卡 | 是 | 是 |
| $U(t),S(t)$ | 使用集合 / 被引用指示(仅反映当前 benchmark 的查询集) | 是 / 是 | 否 |
| $u(t)$ | 未来使用画像(标签) | 是 | 否 |
| $\Delta^{E}_p$ / $\Delta^{F}_p$ | 在场效应($p$ vs 不存,存在轴)/ 表示效应($p$ vs 原文,形式轴) | 是 | 否 |
| $\tilde\tau_p$ | 统一粗筛分(仅剪枝,不训练) | 是 | 否 |
| $\pi_t$ / $b^{\mathrm{rel}}_p$ / $B_p$ / $\Psi_p$ | 被需要的倾向度 / 相关查询上的条件收益 / 相关收益($B_p=\pi_t b^{\mathrm{rel}}_p$)/ 无关查询上的干扰成本 | 是 | 预测可用 |
| $C^{\text{write}}_p,C^{\text{read}}_p,C^{\text{index}}_p$ | 构建 / 读取 / 索引成本;读取按真正进上下文的正文长度 $L^{\mathrm{ctx}}$ 计(卡不进上下文) | 是 | 预测可用 |
| $u(q;b)$ / $g(q;b)$ / $\hat\rho_p$ / $\epsilon_0$ | 端到端答题结局(价值)/ 证据检索分(诊断)/ 检索进前 $k$ 名占比 / 归因护栏阈值(默认 0) | 是 | 预测可用 |
| $V_{\text{store}}(t),V_{\text{none}}(t)$ | 净存储价值 / none 零点(定义) | 是 | 预测可用 |
| $u_p$ | 逐臂结局分(粗筛参考与训练标签来源) | 是 | 否(预测可用) |
| $y_{\text{on}}$ | 排名+引用口径分(离线在线**逐字同式**) | 是 | 是 |
| $n_0$ / $K$ / $k_{\text{draft}}$ | 层内收缩强度(§4.1.2)/ σ 集成数 / 短名单草稿数 | 否 | 是 |
| $(\hat y_b,\hat\sigma)$ / $(\Delta\hat E_p,\hat\rho_p,\hat\sigma_p)$ / $\hat u$ | 第一关收益与不确定度 / 第二关逐臂效应、命中率与不确定度 / 可选画像辅助 | 是 | 是 |
| $\lambda_w,\lambda_r,\lambda_s$ / $\kappa_E$ / $\kappa_F$ / $\theta_{\mathrm{fid}}$ / $\epsilon_0$ | 决策配置(validation 选定的普通超参) | 否 | 是 |
| $\kappa_E$ | 存在轴保守系数(DUM:没把握就存) | 是 | 是 |
| $\hat\sigma_{\text{hi}}$ | 高不确定组阈值(超过则 $\kappa_E$ 上调,§4.5) | 否 | 是 |
| $\tilde Q(t)$ | 生成的可能问题 | 是 | 是 |

## 附录 C 流程步骤

**A 离线测量**:A0 划分与冻结 → A1 反转 evidence 得 $U(t)$ 与共读图 → A2 造画像 $u(t)$(可选辅助)→ A3 确定性生成可能问题(贪心 + 合格性过滤)→ A4 按契约构建全部单元 → A5 层 1 统一粗筛 → A6 同场答题与引用实验($u_p$、$y_{\text{on}}$)→ A7 层 2a 单条替换五步成签(配对 → 分层 → 收缩 → 合成 → 分量入库;边界记忆优先)→ A7b 层 2b 多背景复测(10–20% 子样本,审计)→ A8 层 3 策略背景一次性复测(审计)→ A9 保真证书 → A10 随机混存审计。

**B 训练**:B1 特征与标准化(时间因果快照:特征一律按 $t^-$ 可见信息重算,线上线下同一份代码)→ B2 第一关回归最优臂收益 $Y_b$,并由折间/分位数离散度给出 $\hat\sigma$;成本走确定性计算器 $\bar C$ → B3 第二关训练集(全部有标签样本,不做 gate 筛选)→ B4 pooled GBM 逐臂学习 $\Delta E_p$ 与 $\rho_p$(M/Q/I/E + arm 特征 + 草稿统计;具体 boosting 实现由 validation 确定)→ B5 对话分组训练与条内排序审计,未测格用 mask 跳过 → B6 早停与校准(期望遗憾、排序准确率、校准 slope/intercept 同报;Ridge/NS/MLP 作审计基线)。

**C 决策配置**:C1 $\lambda_w,\lambda_r,\lambda_s$ 用验证集扫效用前沿 → C2 $\kappa_E,\kappa_F$ 用验证集选保守度 → C3 $\theta_{\mathrm{fid}}$(预置 0.95)、$\epsilon_0$(预置 0)定工程标准 → C4 配置与模型版本绑定登记(SHA-256)。

**D 在线写入**:D1 第一关特征(嵌入复用 + 一次 ANN 探针取廉价 I/E 索引状态,零生成调用)→ D2 none 分支(净账不大于零;零热索引足迹 + 冷归档 + η 影子)→ D3 生成问题 + 短名单草稿构建(220M,仅放行轮)→ D4 契约 + 证书预检(弃不合格草稿并记日志)→ D5 pooled 逐臂打分 → D6 第二行 argmax(落 `raw` 即存原文)→ D7 执行草稿 + 输出决策附件。

**E 在线闭环**:E1 解析引用得 $y_{\text{on}}$ → E2 探索(可行集内 5% 随机 + 影子索引 + 影子增强)→ E3 影子对照(同题配对补跑"若存原文")→ E4 DR 纠偏(含挤位环境特征;长尾自归一化)→ E5 回流与攒批转正 → E6 微调门禁(四指标,任一恶化回滚)→ E7 周期重训与原子切换(staging 观测转正,模型与配置同版本更新;漂移做看板监测)。

## 附录 D 超参总表

| 项 | 含义 | 取值 | 出处 |
|---|---|---|---|
| $d_e$ | 检索器向量维度 | 由检索器定 | §4.2.3 |
| $\tau_u$ / 邻居数 / evidence 上限 | 画像构造 | 0.1 / 32 / 12 | §4.1.1 |
| $k$ / 排名窗口 | 条级 top-k / 观测窗口 | 10 / 20 | §3.3 |
| $\beta$ / $f(\cdot)$ | 覆盖权重 / 排名折扣 | 0.5 / $1/(r+2)$ | §3.3 |
| $\epsilon_0$ | 归因护栏阈值 | 0(预置;消融见 §7.3.4) | §3.3 / §7.3.4 |
| 索引卡契约 | ≤64 词/轮(kv ≤4×16;event ≤3×20;graph ≤3×20;hq ≤2×40) | | §3.2 |
| 摘要契约 | ≤60%,句边界按显著性打包 | | §3.2 |
| $\theta_{\mathrm{fid}}$ | 保真证书工程标准(硬验收,非统计保证) | 0.95(预置,敏感性报告) | §4.3 |
| $\gamma$ | $y_{\text{on}}$ 引用项权重 | 0.5 | §4.1.2 |
| $\epsilon$ | 粗筛剪枝阈值 | −0.05(扫描并报告召回率) | §4.1.2 |
| $\pi_t$(估计) | $t$ 被需要的倾向度 | 在 $\mathcal D_Q$ 上的命中频率 + 敏感性 | §3.4 |
| $\tau_c$ | InfoNCE 温度(可选画像辅助) | 0.07 | §4.2.3 |
| 损失权重 | 效应回归 / 条内排序 / 成本回归 / 画像辅助(可选) | 1.0 / 0.5 / 0.3 / 0.1 | §4.2.3 |
| 学习率/轮数/批 | 训练 | 3e-4 / 20+30 / 8 对话×16 轮 | §4.2.3 |
| $K$(集成)/ $k_{\text{draft}}$ / $n_0$ | σ 集成数 / 短名单草稿数 / 收缩强度 | 5 / 2–3 / 验证集 | §4.2.3 / §4.4 / §4.1.2 |
| $\eta$ | none 影子索引比例 / 在线探索比例 | 5% / 5% | §3.5 / §4.4 |
| $\lambda_w,\lambda_r,\lambda_s$ | 写入 / 读取 / 索引效用系数 | 验证集扫前沿 | §4.3 |
| $\kappa_E$ / $\kappa_F$ | 存在轴保守系数 / 形式轴风险厌恶 | 验证集选 | §4.3 / §4.5 |
| $\tau$(分位数) | σ 备选口径(分位数回归时) | 0.1 / 0.5 / 0.9 | §4.2.3 |

## 附录 E 工件规格

必须整体部署、原子切换,缺一不可:`config.json`(含全部决策参数)、`model.pt`、`features.joblib`(标准化统计量)、`staging_buffer.json`;模型与配置用 SHA-256 绑定版本(若启用 kNN 案例基线,`case_memory.json` 同属该版本组)。决策参数 $(\lambda,\kappa,\theta)$ 变更视为新配置,需重新走 validation 后登记,禁止线上混用不同版本组件。

## 附录 F DUM 覆盖机制

DUM(写入时显著性预测不了未来用途,§4.5)由两条互补路径覆盖:

- **写侧(事前预测)**:存在门判决取 $\hat y_b-\bar C(x;\lambda)+\kappa_E\,\hat\sigma$——没把握就存,向保留方向偏;预测不确定度高于阈值的高不确定组把 $\kappa_E$ 上调(分组保守策略,非统计保证);判 `none` 的记忆进冷归档,并按 $\eta{=}5\%$ 保留影子索引继续观测。
- **读侧(事后兜底)**:当一次查询的读侧分数低(检索自信不足)时,向冷归档扩召回,把"当年判 none、如今被问到"的记忆重新捞回——MemSIF 式的事后追认由此被吸收进读侧兜底,与写侧预测对接,形成"事前预测 + 事后兜底"的完整覆盖。

真正外生的随机用途仍是信息论上界,以上机制只缓解不消除(§8.1)。

## 附录 G 数据说明(MSC,待数据核查)

MSC 在本论文中作为补充数据集,用于检验结论跨数据分布的稳健性(§6.1)。其原始出处与具体轮次/查询构成在数据核对阶段一并登记,并在题名中注明"数据来源待批注"。本论文不以 MSC 作为独立结论的支撑——主结论全部来自 LoCoMo(主)与 LongMemEval(-S)(辅测)。凡引用 MSC 数字处一律标注【待实测】或"预期",且不当作独立证据。

## 附录 H 无 gold evidence 的实验(检验"是否可迁移到真实 agent 日志")

主训练依赖 benchmark 提供的 $E(q)\subseteq T$(哪些轮次是某问题的证据),但真实部署日志通常**没有**这种标注。这直接决定 R2W 看起来是"LoCoMo 专用方法",还是"可迁移到真实 agent 日志的框架"。为此做一个**附录实验**,把 evidence 标注替换为三种自动来源,考察 $\Delta^E_p/\Delta^F_p$ 标签与最终 F1 的退化:

| 证据来源 | 说明 | 相关性 | 预期退化 |
|---|---|---|---|
| gold evidence(基准) | benchmark 自带 | 高 | — |
| reader 生成引用 | 让阅读模型在离线标注"哪几条被它读到/引用" | 中 | 若退化明显,说明 R2W 依赖 gold |
| 生成伪问题归因 | 用 §4.1.2 生成的可能问题,反推"哪几轮相关" | 高低待测 | 决定能否在无标注日志自举 |

**判读**:若 gold → reader-引用 → 伪问题归因这条路径上 F1 没有剧烈坍塌,则说明 R2W 的核心(用可重建索引 + 便宜写策略)并不依赖手工 evidence 标注,可以迁移;若坍塌,则在结论如实说明"高保真标签是 R2W 的前提之一",并把"如何从日志自获 evidence"列为明确未来工作。这一步对判断方法的适用面很重要,建议至少放附录。
